---
phase: 41-fix-the-reminder-system-to-avoid-stale-reminders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/tool-handler.ts
  - tests/ai/tool-handler.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "When a user changes a meal plan (e.g. swaps Wednesday dinner from Pasta to Tacos), reminders are regenerated to reflect the new meal"
    - "When a meal is removed from a plan, phantom reminders for that meal no longer fire"
    - "Feedback check-ins are also regenerated when a meal plan changes, since they reference specific meals"
  artifacts:
    - path: "src/ai/tool-handler.ts"
      provides: "generateRemindersFn call after successful save_meal_plan"
      contains: "generateRemindersFn(householdId)"
    - path: "tests/ai/tool-handler.test.ts"
      provides: "Test verifying reminder regeneration on plan save"
      contains: "generateRemindersFn"
  key_links:
    - from: "src/ai/tool-handler.ts"
      to: "generateRemindersFn"
      via: "function call after planRepository.savePlan"
      pattern: "generateRemindersFn\\(householdId\\)"
---

<objective>
Call generateRemindersFn after save_meal_plan to prevent stale reminders

Purpose: When a user modifies their meal plan, old reminders (morning_summary, prep_alert, start_cooking, feedback_checkin) retain stale context_json referencing the previous meals. By calling generateRemindersFn after a successful plan save, all future reminders are deleted and rebuilt from the current plan data.

Output: Patched tool-handler.ts with reminder regeneration on plan save, plus a test proving it works
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/ai/tool-handler.ts
@tests/ai/tool-handler.test.ts
@src/reminders/generator.ts
@src/feedback/generator.ts
@src/main.ts (lines 123-143 showing regenerateReminders wiring)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generateRemindersFn call after save_meal_plan success</name>
  <files>src/ai/tool-handler.ts</files>
  <action>
In the `save_meal_plan` case (around line 691-724), after the successful `planRepository.savePlan()` call and the conflict check (lines 696-702), add a call to `generateRemindersFn(householdId)` if the function is available. Place it AFTER the conflict check but BEFORE building the response object.

The exact pattern to follow already exists in the `update_reminder_settings` case at lines 1065-1068:

```typescript
// Regenerate reminders after settings change
if (generateRemindersFn) {
  generateRemindersFn(householdId);
}
```

Insert the same pattern after line 702 (after the conflict/null check for `plan`):

```typescript
// Regenerate reminders after plan change to avoid stale reminder context
if (generateRemindersFn) {
  generateRemindersFn(householdId);
}
```

This single call handles BOTH regular reminders (morning_summary, prep_alert, start_cooking) AND feedback check-ins, because `generateRemindersFn` is wired in main.ts to call both `generateReminders()` and `generateFeedbackCheckins()`.

Do NOT add the call when `plan` is null (conflict case) -- reminders should only regenerate on successful saves.
  </action>
  <verify>Run `npm run typecheck` to confirm no type errors. Visually confirm the call is placed after the null/conflict check and before the response construction.</verify>
  <done>The save_meal_plan case calls generateRemindersFn(householdId) after every successful plan save, matching the established pattern from update_reminder_settings.</done>
</task>

<task type="auto">
  <name>Task 2: Add test verifying reminder regeneration on plan save</name>
  <files>tests/ai/tool-handler.test.ts</files>
  <action>
Add a new test to the existing `describe("tool-handler save_meal_plan")` block in `tests/ai/tool-handler.test.ts`.

The test needs a mock `generateRemindersFn` passed to `createToolHandler`. Create a small helper or extend the existing `createMockDeps()` to accept an optional `generateRemindersFn` mock.

Test: "calls generateRemindersFn after successful save":
1. Create a `vi.fn()` mock for generateRemindersFn
2. Create a tool handler with that mock in deps (along with the existing mock planRepository)
3. Call `handler.handleToolCall("save_meal_plan", { week_start_date: "2026-02-16", entries: [{ day: 0, recipe_name: "Tacos" }] })`
4. Assert `generateRemindersFn` was called exactly once with the householdId ("test-household")

Also add a negative test: "does not call generateRemindersFn on conflict":
1. Set `mockPlanRepository.savePlan` to return `null` (simulating a conflict)
2. Call save_meal_plan
3. Assert `generateRemindersFn` was NOT called

Use the same mock construction pattern as `createMockDeps()` but add the `generateRemindersFn` to the deps object. The existing `createMockDeps()` does NOT pass `generateRemindersFn`, so create a new helper `createMockDepsWithReminders()` or add the mock inline in each test.
  </action>
  <verify>Run `npm test -- tests/ai/tool-handler.test.ts` and confirm both new tests pass. Also confirm all existing tests in the file still pass.</verify>
  <done>Two new tests exist: one proving generateRemindersFn is called on successful save, one proving it is NOT called on conflict. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with no errors
2. `npm test -- tests/ai/tool-handler.test.ts` -- all tests pass including the 2 new ones
3. `npm test` -- full test suite passes
4. Visual code review: the `generateRemindersFn(householdId)` call is inside the `save_meal_plan` case, after the conflict check, guarded by `if (generateRemindersFn)`
</verification>

<success_criteria>
- save_meal_plan calls generateRemindersFn after every successful plan save
- Conflict/null saves do NOT trigger reminder regeneration
- Two new tests validate both positive and negative cases
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/41-fix-the-reminder-system-to-avoid-stale-reminders/41-01-SUMMARY.md`
</output>
