# Deferred Items - Phase 48

## Pre-existing Test Failures

- `tests/notifications/update-notifier.test.ts` - 3 tests fail due to stale release note fixture data (tests expect "new tricks" but actual release notes have changed). Not related to phase 48 changes.
