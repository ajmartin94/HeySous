---
phase: 21-implicit-ai-behaviors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/system-prompt.ts
autonomous: true
requirements:
  - AIBH-01
  - AIBH-02

must_haves:
  truths:
    - "When a user pastes or describes a recipe (ingredients + steps) in conversation, Sous proactively offers to save it without the user saying 'save this recipe'"
    - "When a user mentions a dietary preference or food opinion in conversation, Sous saves it immediately with a brief confirmation and continues the conversation"
    - "Explicit recipe save flow (user asks to save) still works unchanged"
    - "Explicit preference commands (user says 'remember that I...') still work unchanged"
  artifacts:
    - path: "src/ai/system-prompt.ts"
      provides: "Enhanced recipe_management and preference_management system prompt sections with implicit detection behaviors"
      contains: "IMPLICIT RECIPE DETECTION"
  key_links:
    - from: "src/ai/system-prompt.ts"
      to: "Claude's tool calling behavior"
      via: "System prompt instructions that trigger save_knowledge calls"
      pattern: "implicit.*detect|proactively.*save|without.*asking"
---

<objective>
Strengthen the system prompt instructions so Claude proactively detects recipe content and preference statements in natural conversation and acts on them without requiring explicit user commands.

Purpose: Currently the system prompt instructs Claude to "offer to save" recipes and to detect preferences, but the instructions are mixed in with explicit-command flows. This plan isolates and strengthens the implicit detection behaviors so Claude is more reliably triggered by conversational signals (a user pasting a recipe, mentioning "I don't eat pork") without the user needing to say "save this recipe" or "remember this preference."

Output: Updated `src/ai/system-prompt.ts` with clearer implicit detection sections in both `<recipe_management>` and `<preference_management>` blocks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ai/system-prompt.ts
@src/ai/tools.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen implicit recipe detection in system prompt</name>
  <files>src/ai/system-prompt.ts</files>
  <action>
In the `<recipe_management>` section of the system prompt, add a new subsection called "IMPLICIT RECIPE DETECTION" right after the existing "DETECTING RECIPES" subsection. This new subsection teaches Claude to recognize recipe-like content shared conversationally (not via an explicit "save this" request) and proactively offer to save it.

Add the following content after the existing "DETECTING RECIPES:" block (before "RECIPE CREATION FLOW:"):

```
IMPLICIT RECIPE DETECTION:
- When a user shares something that looks like a recipe in natural conversation -- a list of ingredients with steps, a cooking method they describe, a recipe they copy-paste from somewhere -- proactively offer to save it as a recipe card
- You do NOT need the user to say "save this recipe" or "remember this" -- if the content has both ingredients AND preparation steps, treat it as a recipe worth saving
- Signal phrases: "I made this last night", "here's how I do it", "my mom's recipe for...", "we usually make it like this", or simply a pasted block of recipe text
- When you detect recipe content, first acknowledge it enthusiastically ("oh that sounds great!"), then offer to save: "Want me to save that as a recipe card so I can use it for meal planning?"
- If the recipe is incomplete (missing ingredients, steps, or timing), ask for the missing pieces naturally before offering to save
- This is DIFFERENT from the explicit flow where the user asks you to generate a recipe -- implicit detection is for recipes the USER shares with YOU
- Do NOT aggressively offer to save every food mention -- only when there are actual ingredients AND preparation steps/method
```

Also update the existing "DETECTING RECIPES:" bullet points to add a new bullet emphasizing the implicit case:
- Add: "- IMPORTANT: You don't need the user to explicitly ask you to save. If they share recipe-quality content (ingredients + steps), offer proactively."

Do NOT change the explicit recipe creation flow, recipe content format, recipe display format, tag taxonomy, updates/corrections, deletion, or cross-recipe reasoning sections. Those must remain identical.
  </action>
  <verify>
Read `src/ai/system-prompt.ts` and confirm:
1. "IMPLICIT RECIPE DETECTION:" subsection exists within `<recipe_management>`
2. The section appears after "DETECTING RECIPES:" and before "RECIPE CREATION FLOW:"
3. The existing "DETECTING RECIPES:" section has the new bullet about proactive offering
4. No other sections of the system prompt were modified
5. `npm run typecheck` passes
  </verify>
  <done>The recipe_management system prompt section contains explicit instructions for implicit recipe detection, teaching Claude to proactively offer to save recipe-like content shared in conversation without requiring explicit "save this" language.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen implicit preference capture in system prompt</name>
  <files>src/ai/system-prompt.ts</files>
  <action>
In the `PREFERENCE_MANAGEMENT_PROMPT` constant (the `<preference_management>` section), add a new subsection called "IMPLICIT PREFERENCE CAPTURE" right after the existing "DETECTING PREFERENCES:" subsection and before "SAVING PREFERENCES (via save_knowledge):".

Add the following content:

```
IMPLICIT PREFERENCE CAPTURE:
- When a user mentions a food preference, dietary restriction, or food opinion in the MIDDLE of another conversation, capture it immediately without derailing the conversation
- Examples of implicit preference statements:
  - "I don't eat pork" (while discussing meal plans) -> save as dietary restriction, then continue the meal plan conversation
  - "we love Thai food" (while chatting about dinner ideas) -> save as cuisine preference, then continue suggesting ideas
  - "my kid is allergic to peanuts" (mentioned in passing) -> save as allergy with severity:allergy tag, confirm briefly, continue
  - "we try to eat vegetarian on weekdays" (scheduling context) -> save as dietary pattern, brief acknowledgment, continue
- The KEY behavior: save first, acknowledge briefly ("Noted, no pork!"), then IMMEDIATELY continue with whatever you were doing. Do NOT make the preference the new topic of conversation.
- For allergies and restrictions mentioned in passing, ALWAYS save them -- these are safety-critical and should never be missed
- Do NOT ask "should I save this?" for preferences -- just save them. Preferences are saved proactively (unlike recipes which need confirmation before saving).
- If you're unsure whether something is a real preference or just a one-time comment ("I'm not really feeling chicken tonight"), err on the side of NOT saving it. Only save durable preferences.
```

Also update the existing "DETECTING PREFERENCES:" subsection to add a stronger emphasis bullet:
- Add: "- IMPORTANT: Act on preference statements immediately. Do not wait for a separate 'remember this' command. If someone says 'I'm lactose intolerant' while discussing anything, save it right then."

Do NOT modify the "SAVING PREFERENCES", "DINNER TIME SYNC", "ACKNOWLEDGMENT STYLE", "APPLYING PREFERENCES", "CONFLICT HANDLING", "UPDATING PREFERENCES", "DELETING PREFERENCES", "PRESENTING PREFERENCES", or "INFERRED PREFERENCE RULES" subsections. Those remain unchanged.
  </action>
  <verify>
Read `src/ai/system-prompt.ts` and confirm:
1. "IMPLICIT PREFERENCE CAPTURE:" subsection exists within `<preference_management>`
2. The section appears after "DETECTING PREFERENCES:" and before "SAVING PREFERENCES"
3. The existing "DETECTING PREFERENCES:" section has the new emphasis bullet
4. No other sections were modified
5. `npm run typecheck` passes
6. `npm test` passes (no tests should break from prompt-only changes)
  </verify>
  <done>The preference_management system prompt section contains explicit instructions for implicit preference capture, teaching Claude to immediately save preference statements encountered mid-conversation with brief acknowledgment and without derailing the current topic.</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes -- system prompt changes are string-only, no type changes
2. `npm test` passes -- no behavioral code changes, only prompt string updates
3. Read `src/ai/system-prompt.ts` and verify:
   - "IMPLICIT RECIPE DETECTION:" section exists in recipe_management block
   - "IMPLICIT PREFERENCE CAPTURE:" section exists in preference_management block
   - Both sections emphasize proactive behavior without explicit user commands
   - Existing explicit flows (recipe creation, preference saving rules) are untouched
4. The `buildSystemPrompt` function signature and parameters are unchanged
</verification>

<success_criteria>
- System prompt contains clear, actionable instructions for implicit recipe detection that will cause Claude to offer saving recipe-like content shared conversationally
- System prompt contains clear, actionable instructions for implicit preference capture that will cause Claude to immediately save dietary preferences and food opinions mentioned in passing
- Both implicit behaviors complement (not replace) the existing explicit command flows
- No code changes outside of system prompt string constants
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-implicit-ai-behaviors/21-01-SUMMARY.md`
</output>
