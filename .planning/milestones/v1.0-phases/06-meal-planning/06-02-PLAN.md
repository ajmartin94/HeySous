---
phase: 06-meal-planning
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/ai/tools.ts
  - src/ai/tool-handler.ts
  - src/ai/system-prompt.ts
autonomous: true

must_haves:
  truths:
    - "Claude has planning tools available in tool use loop"
    - "Tool handler dispatches save_meal_plan, get_meal_plan, log_meal, get_cooking_history calls to planning repository and history functions"
    - "System prompt includes meal planning instructions teaching Claude how to propose, adjust, and display plans"
    - "System prompt includes injected active plan and cooking history context"
  artifacts:
    - path: "src/ai/tools.ts"
      provides: "PLAN_TOOLS array with 4 tool definitions"
      contains: "PLAN_TOOLS"
    - path: "src/ai/tool-handler.ts"
      provides: "Plan tool dispatch cases in handleToolCall switch"
      contains: "save_meal_plan"
    - path: "src/ai/system-prompt.ts"
      provides: "Meal planning system prompt section and plan context injection"
      contains: "meal_planning"
  key_links:
    - from: "src/ai/tool-handler.ts"
      to: "src/planning/repository.ts"
      via: "import and call repository methods"
      pattern: "planRepository"
    - from: "src/ai/tool-handler.ts"
      to: "src/planning/history.ts"
      via: "import and call history functions"
      pattern: "logMeal|getCookingHistory"
    - from: "src/ai/system-prompt.ts"
      to: "src/planning/context.ts"
      via: "import buildPlanContext for prompt injection"
      pattern: "buildPlanContext"
---

<objective>
Add Claude's planning tools (save_meal_plan, get_meal_plan, log_meal, get_cooking_history), wire tool handler dispatch for plan operations, and add meal planning intelligence to the system prompt with active plan context injection.

Purpose: This gives Claude the ability to create, read, and modify meal plans through tool calls, and the instructions for how to conduct planning conversations. Without this, Claude has no way to interact with the planning data layer from Plan 01.

Output: Three modified files in src/ai/ -- tools.ts gets PLAN_TOOLS, tool-handler.ts gets plan dispatch cases, system-prompt.ts gets planning instructions and context injection.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-meal-planning/06-RESEARCH.md
@.planning/phases/06-meal-planning/06-CONTEXT.md
@.planning/phases/06-meal-planning/06-01-SUMMARY.md
@src/ai/tools.ts
@src/ai/tool-handler.ts
@src/ai/system-prompt.ts
@src/ai/claude-client.ts
@src/planning/repository.ts
@src/planning/history.ts
@src/planning/context.ts
@src/planning/date-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan tool definitions and tool handler dispatch</name>
  <files>
    src/ai/tools.ts
    src/ai/tool-handler.ts
  </files>
  <action>
**In src/ai/tools.ts**, add a new exported array `PLAN_TOOLS: Anthropic.Tool[]` with 4 tool definitions:

1. `save_meal_plan`:
   - Description: "Save or update a weekly meal plan. Provide the week start date (Monday) and an array of meal entries. Each entry needs a day (0=Mon through 6=Sun), meal type, and recipe name. Replaces all entries for that week -- always send the COMPLETE plan, not just changes."
   - Input schema:
     - week_start_date (string, required): "Monday of the plan week in ISO format: YYYY-MM-DD"
     - entries (array, required): items with properties: day (number, required, "0=Monday through 6=Sunday"), meal_type (string, enum ["breakfast","lunch","dinner"], "Meal type, default: dinner"), recipe_name (string, required, "Name of the recipe or meal"), knowledge_item_id (number, optional, "Optional ID of stored recipe in knowledge base")

2. `get_meal_plan`:
   - Description: "Get the meal plan for a specific week. If no week specified, returns the current week's plan. Returns all entries for the week."
   - Input schema:
     - week_start_date (string, optional): "Monday of the plan week in ISO format: YYYY-MM-DD. Omit for current week."
   - required: [] (empty -- week_start_date is optional)

3. `log_meal`:
   - Description: "Log a meal to cooking history. Use for unplanned meals the user mentions ('we had pizza tonight') or corrections to planned meals. Planned meals are auto-logged when the day passes."
   - Input schema:
     - recipe_name (string, required): "Name of the meal/recipe"
     - cooked_date (string, required): "Date the meal was cooked in ISO format: YYYY-MM-DD"
     - meal_type (string, optional, enum ["breakfast","lunch","dinner"]): "Meal type (default: dinner)"
     - knowledge_item_id (number, optional): "Optional ID of stored recipe"
     - notes (string, optional): "Optional notes about the meal"
   - required: ["recipe_name", "cooked_date"]

4. `get_cooking_history`:
   - Description: "Get recent cooking history. Returns what was cooked/planned in the date range. Defaults to the last 3 weeks if no range specified."
   - Input schema:
     - start_date (string, optional): "Start of date range in ISO format: YYYY-MM-DD"
     - end_date (string, optional): "End of date range in ISO format: YYYY-MM-DD"
   - required: [] (empty -- both optional)

Keep KNOWLEDGE_TOOLS unchanged. PLAN_TOOLS is a separate export.

**In src/ai/tool-handler.ts**, extend the tool handler to support plan operations:

1. Update the `createToolHandler` function signature to accept additional dependencies:
   - Add `planRepository` (type: ReturnType<typeof createPlanRepository>) as an optional dependency
   - Add `sqlite` (type: BetterSqlite3.Database) for cooking history functions
   - Keep existing dependencies unchanged for backward compatibility

2. Add four new cases to the switch statement in `handleToolCall`:

   `save_meal_plan`: Extract week_start_date and entries from input. Map entries to PlanEntry format (defaulting meal_type to "dinner"). Call planRepository.savePlan(chatId, weekStartDate, entries). Return JSON with message "Saved plan for week of {weekStartDate}" and the plan data.

   `get_meal_plan`: Extract optional week_start_date. If not provided, use getWeekStartDate() from date-utils. Call planRepository.getPlan(chatId, weekStartDate). If no plan found, return JSON with message "No plan found for week of {weekStartDate}". Otherwise return JSON with plan data including formatted entries.

   `log_meal`: Extract recipe_name, cooked_date, and optional fields. Call logMeal(sqlite, { chatId, recipeName, cookedDate, mealType, knowledgeItemId, notes, source: "unplanned" }). Return JSON with message "Logged {recipeName} for {cookedDate}".

   `get_cooking_history`: Extract optional start_date and end_date. Call getCookingHistory(sqlite, chatId, startDate, endDate). Return JSON with history entries.

3. Guard plan tool cases: if planRepository is not provided (backward compat), return "Plan tools not available" for plan-related tool names.

Import: createPlanRepository type from "../planning/repository.js", logMeal and getCookingHistory from "../planning/history.js", getWeekStartDate from "../planning/date-utils.js", type BetterSqlite3 from "better-sqlite3".
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify PLAN_TOOLS has 4 entries. Verify tool-handler.ts switch has cases for save_meal_plan, get_meal_plan, log_meal, get_cooking_history.
  </verify>
  <done>
Four planning tools defined and exported. Tool handler dispatches all four plan tools to the planning repository and history functions. Backward compatible -- existing tool handler callers still work without plan dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: System prompt planning instructions and context injection</name>
  <files>src/ai/system-prompt.ts</files>
  <action>
Update `src/ai/system-prompt.ts` to add meal planning intelligence and context injection:

1. **Update buildSystemPrompt signature** to accept an optional `planContext` parameter:
   `buildSystemPrompt(preferences?: PreferenceSummary[], planContext?: string): string`

   If planContext is provided (non-empty string), append it after the preference sections and before the closing of the prompt. Place it after PREFERENCE_MANAGEMENT_PROMPT.

2. **Add MEAL_PLANNING_PROMPT constant** (same pattern as PREFERENCE_MANAGEMENT_PROMPT -- always included):

```
<meal_planning>
You help users plan their weekly meals through natural conversation. Planning is collaborative -- you propose, they react, you iterate until it feels right.

CREATING A PLAN:
- When the user asks for a meal plan, search their recipes and cooking history first (via tools)
- Propose a full Monday-Sunday dinner plan in one message
- Use their stored recipes when possible, but freely suggest new ideas too
- Consider their preferences (dietary restrictions, household info) from the preference context
- If cooking history is available, use it as context -- but don't apply rigid rotation or recency logic
- Only factor in effort/complexity if the user specifically mentions it ("something easy on Tuesday")
- Do NOT auto-optimize for variety -- the user drives choices

PLAN DISPLAY FORMAT:
- Show the full plan in a single message
- Format: recipe name only per day, clean and minimal
- Use this structure for dinner-only plans:

<b>This Week's Plan</b>
<i>{date range}</i>

Monday - {recipe}
Tuesday - {recipe}
Wednesday - {recipe}
Thursday - {recipe}
Friday - {recipe}
Saturday - {recipe}
Sunday - {recipe}

- For plans with multiple meal types per day, group by day:

<b>This Week's Plan</b>
<i>{date range}</i>

<b>Monday</b>
Breakfast - {recipe}
Lunch - {recipe}
Dinner - {recipe}

ADJUSTING PLANS:
- Apply changes IMMEDIATELY without confirmation -- "swap Thursday to tacos" -> update plan and show revised version
- Show the full updated plan after every change
- No finalize step -- plans are living objects, always open for changes
- If the conversation naturally winds down, you may suggest "looks like a good week!" but never lock the plan
- Multiple active plans are supported (this week AND next week)

USING PLAN TOOLS:
- save_meal_plan: Always send the COMPLETE plan (all entries), not just changes. The tool replaces all entries for that week.
- get_meal_plan: Use for explicit plan retrieval. For casual references ("what's for dinner tonight"), use the plan context already in this prompt instead.
- log_meal: Use when user mentions an unplanned meal ("we had pizza tonight"). Planned meals are auto-logged.
- get_cooking_history: Use when you need historical context beyond what's in this prompt (the prompt includes last 3 weeks).

DAY AND DATE HANDLING:
- Default to the current week when references are ambiguous
- If the user has multiple active plans and the target week is unclear, ask for clarification
- Always use ISO dates (YYYY-MM-DD) in tool calls, never day names
- Resolve "this Thursday" vs "next Thursday" based on conversation context -- if unclear, ask

COOKING HISTORY:
- When the user mentions cooking something unplanned ("we had pizza tonight"), log it with log_meal
- When asked "what did we eat last week?", use get_cooking_history or reference the context in this prompt
- History is context for your suggestions, not a constraint -- no rigid recency/rotation logic
</meal_planning>
```

3. **Append planContext and MEAL_PLANNING_PROMPT** in the buildSystemPrompt return string:
   - After `${PREFERENCE_MANAGEMENT_PROMPT}`, add `${planContext || ""}${MEAL_PLANNING_PROMPT}`
   - This ensures the plan context (active plans + cooking history) is available alongside the planning instructions

4. **Update the <tools> section** to mention planning tools:
   - Add to the existing tools list: "You can also SAVE and RETRIEVE meal plans using save_meal_plan, get_meal_plan, log_meal, and get_cooking_history"
   - Keep existing tool guidance unchanged
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify buildSystemPrompt accepts optional planContext parameter. Verify MEAL_PLANNING_PROMPT constant exists. Verify the <tools> section mentions planning tools.
  </verify>
  <done>
System prompt includes comprehensive meal planning instructions covering: plan creation approach (conversational not algorithmic), display format (recipe name only, Monday-Sunday, single message), adjustment flow (immediate, no confirmation), tool usage guidance, day/date handling, and cooking history behavior. Plan context is injectable via the planContext parameter. All per user decisions: no auto-optimization, no confirmation flow, no finalize step, no rigid selection logic.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. PLAN_TOOLS exported from tools.ts with 4 tool definitions
3. tool-handler.ts handles save_meal_plan, get_meal_plan, log_meal, get_cooking_history
4. system-prompt.ts includes MEAL_PLANNING_PROMPT section
5. buildSystemPrompt accepts optional planContext parameter
6. No new dependencies added
</verification>

<success_criteria>
- Claude has 4 planning tools available (save_meal_plan, get_meal_plan, log_meal, get_cooking_history)
- Tool handler routes all 4 tools to planning repository and history functions
- System prompt teaches Claude conversational planning approach per user decisions
- System prompt handles active plan context injection
- Plan display format matches locked decisions (recipe name only, Monday-Sunday, single message)
- No confirmation flow, no finalize step, no auto-optimization in prompt instructions
</success_criteria>

<output>
After completion, create `.planning/phases/06-meal-planning/06-02-SUMMARY.md`
</output>
