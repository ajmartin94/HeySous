---
status: complete
phase: v1.4-backlog-sweep
source: 25-01-SUMMARY.md, 26-01-SUMMARY.md, 27-01-SUMMARY.md, 28-01-SUMMARY.md, 29-01-SUMMARY.md, 30-01-SUMMARY.md
started: 2026-02-20T12:00:00Z
updated: 2026-02-20T17:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Server starts with migrations
expected: Run `npm run dev`. Server starts without errors. No migration failures in console output (migrations run automatically on boot via PRAGMA user_version).
result: pass

### 2. Duplicate recipe detection
expected: Tell Sous about a recipe that already exists in your knowledge base (e.g., repeat a recipe you previously saved). Sous should say something like "I already have something similar" and present the existing match, asking whether to update the existing one or save as new.
result: pass

### 3. Duplicate recipe bypass
expected: After Sous shows a duplicate match (test 2), tell it to "save as new". Sous should save the recipe without further dedup prompts.
result: pass

### 4. Bot messages have Sous personality
expected: Trigger any bot-initiated message (e.g., send a message that causes an error or timeout, or check the access gate message for an unregistered user). The message should sound conversational and Sous-like (no "Error:", no "Access denied" -- warm, personality-driven phrasing).
result: pass

### 5. Message variation
expected: Trigger the same type of bot message multiple times (e.g., multiple errors, or restart and check welcome messages). Phrasing should vary between occurrences -- not identical text every time.
result: pass

### 6. Recipe URL import
expected: Send a recipe URL (e.g., from AllRecipes, Serious Eats, or any recipe site). Within ~15 seconds, Sous extracts and presents the recipe title, ingredients, and instructions, and asks you to confirm before saving.
result: pass

### 7. Confirm URL import saves recipe
expected: After Sous presents an extracted recipe from a URL (test 6), confirm it. The recipe should be saved to your knowledge base with the source URL retained.
result: pass (fixed: single-turn save flow)

### 8. Bad URL handling
expected: Send a non-recipe URL (e.g., a news article, a 404 page, or a paywalled site). Sous should produce a helpful message explaining the issue and suggesting alternatives (paste the recipe text, send a photo).
result: pass

### 9. Recipe photo import
expected: Send a photo of a printed or handwritten recipe (cookbook page, recipe card, etc.). Sous extracts the recipe content (title, ingredients, instructions) and presents it for confirmation before saving.
result: pass (fixed: added message:document handler for images sent as files)

### 10. Non-recipe photo handling
expected: Send a non-recipe photo (e.g., a sunset, a selfie, food plating). Sous responds normally without attempting recipe extraction -- no "I couldn't extract a recipe" message, just natural conversation about the photo.
result: skipped
reason: blocked by test 9 photo handler issue

### 11. What's new notification
expected: On the first interaction after the v1.4 update is deployed, Sous shows a "what's new" notification before responding to your message. The notification mentions new capabilities (recipe links, photos, smarter saving) in Sous's conversational voice.
result: pass

### 12. Notification shows once only
expected: After seeing the "what's new" notification (test 11), send another message. No repeated notification -- Sous responds normally without the "what's new" preamble.
result: pass

## Summary

total: 12
passed: 11
issues: 0
pending: 0
skipped: 1

## Gaps

- truth: "Confirmed URL import recipe is saved immediately after user confirms"
  status: failed
  reason: "User reported: had to send a followup message after Sous said it was saved for it to actually get saved"
  severity: major
  test: 7
  root_cause: "Tool interaction context (tool_use/tool_result) is lost between conversation turns. Only plain text is persisted to messages table. When user confirms on turn 2, Claude lacks the structured ImportResult data from turn 1's import_from_url call, so it says 'saved' without actually calling save_knowledge."
  artifacts:
    - path: "src/pipeline/processor.ts"
      issue: "Lines 140-160, 369-376: Only text messages saved/loaded, tool context lost between turns"
    - path: "src/ai/claude-client.ts"
      issue: "Lines 134, 224-231: loopMessages with tool interactions is ephemeral, never returned"
    - path: "src/conversation/context-builder.ts"
      issue: "Lines 96-97: No support for tool_use/tool_result message types"
  missing:
    - "Either persist tool interactions in conversation history, or change system prompt to instruct Claude to reconstruct recipe from displayed text and call save_knowledge, or change to single-turn save flow"
  debug_session: ".planning/debug/url-import-extra-message.md"

- truth: "Photo sent to bot triggers recipe extraction and response"
  status: failed
  reason: "User reported: sent the photo, no response. then sent 'save this' and it referenced the prior message. almost like it's not getting the pic at all"
  severity: major
  test: 9
  root_cause: "bot.start() and setWebhook() called without explicit allowed_updates. Telegram persists allowed_updates server-side from previous calls. A prior session likely set a restrictive list that excluded photo messages. Handler code is correct but updates never arrive."
  artifacts:
    - path: "src/main.ts"
      issue: "Lines 258, 268: No allowed_updates specified in setWebhook() or bot.start()"
  missing:
    - "Explicitly pass allowed_updates: ['message', 'callback_query', 'my_chat_member'] to both bot.start() and bot.api.setWebhook()"
  debug_session: ".planning/debug/photo-handler-never-fires.md"
