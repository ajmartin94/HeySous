---
phase: 52-onboarding-memory-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/onboarding/memory-integration.test.ts
autonomous: false
requirements: [ONBOARD-MEM]

must_haves:
  truths:
    - "Memory tools (save_memory) are available to Claude during onboarding preferences phase"
    - "Onboarding prompt explicitly instructs Claude to call save_memory for each preference category"
    - "Production users who completed onboarding have memories in the memories table"
  artifacts:
    - path: "tests/onboarding/memory-integration.test.ts"
      provides: "Automated verification that memory tools are included in onboarding tool set"
      min_lines: 20
  key_links:
    - from: "src/pipeline/processor.ts"
      to: "src/ai/tools.ts"
      via: "MEMORY_TOOLS spread into allTools"
      pattern: "MEMORY_TOOLS"
    - from: "src/onboarding/prompt.ts"
      to: "save_memory"
      via: "prompt text references save_memory tool"
      pattern: "save_memory"
---

<objective>
Verify that the onboarding preferences phase actually persists user preferences as memories via save_memory, and add a regression test to ensure memory tools remain available during onboarding.

Purpose: The memory system (Phase 49) was added after onboarding (Phase 15). The onboarding prompt was updated to reference save_memory, but we need to confirm the integration works end-to-end: tools are available, prompt references them, and production data confirms memories are saved.
Output: A test file proving the integration, and prod DB confirmation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/pipeline/processor.ts
@src/onboarding/prompt.ts
@src/ai/tools.ts
@src/memory/repository.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression test for memory tool availability during onboarding</name>
  <files>tests/onboarding/memory-integration.test.ts</files>
  <behavior>
    - Test 1: MEMORY_TOOLS array contains save_memory, delete_memory, search_memories tools
    - Test 2: allTools construction in processor includes all MEMORY_TOOLS entries (verify by importing the same arrays and checking they are spread in)
    - Test 3: buildOnboardingPrompt("preferences") output contains "save_memory" instructions for each category (dietary, taste, schedule, logistics, cooking_style)
    - Test 4: buildOnboardingPrompt("preferences") instructs saving memories inline ("don't wait until the end")
  </behavior>
  <action>
Create a test file at tests/onboarding/memory-integration.test.ts that verifies the onboarding-memory integration:

1. Import MEMORY_TOOLS from src/ai/tools.js and verify it contains save_memory, delete_memory, search_memories tool definitions (check the `name` field on each).

2. Import buildOnboardingPrompt from src/onboarding/prompt.js. Call buildOnboardingPrompt("preferences") and verify:
   - The returned string contains "save_memory" (the tool is referenced)
   - The returned string references all expected categories: "dietary", "taste", "schedule", "logistics", "cooking_style"
   - The returned string contains instruction to save memories inline (contains "don't wait until the end" or similar)

3. Verify MEMORY_TOOLS is imported by processor.ts by importing MEMORY_TOOLS and all other tool arrays (KNOWLEDGE_TOOLS, PLAN_TOOLS, etc.) from src/ai/tools.js and confirming MEMORY_TOOLS is a non-empty array with at least 3 tools. This is a static check -- the actual allTools concatenation happens at runtime inside processMessage, but we can verify the building blocks are correct.

Do NOT mock or test the full processor pipeline -- that would require too much setup. Focus on the contract: the tools exist and the prompt references them.
  </action>
  <verify>
    <automated>npx vitest run tests/onboarding/memory-integration.test.ts</automated>
  </verify>
  <done>All tests pass confirming: MEMORY_TOOLS has save_memory/delete_memory/search_memories, preferences prompt references save_memory for all 5 categories, and inline-save instruction is present</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production memories from onboarding</name>
  <files>none</files>
  <action>
Use MCP debug tools (or local DB) to verify that memories exist in production for users who completed onboarding. This confirms the end-to-end integration works, not just the code wiring.

Query: SELECT m.content, m.category, m.created_at FROM memories m JOIN households h ON m.household_id = h.id LIMIT 20

Look for memories with categories matching onboarding topics: dietary, taste, schedule, logistics, cooking_style.
  </action>
  <what-built>Regression test proving onboarding-memory integration is wired correctly at the code level. Now need to confirm production data shows memories saved during onboarding.</what-built>
  <how-to-verify>
    1. Use MCP debug tool to query prod DB: `SELECT m.content, m.category, m.created_at FROM memories m JOIN households h ON m.household_id = h.id LIMIT 20`
    2. Verify memories exist with categories matching onboarding topics (dietary, taste, schedule, logistics, cooking_style)
    3. If memories exist for onboarded users: phase is confirmed working, no code changes needed
    4. If NO memories exist: report back and we will investigate whether save_memory calls are failing silently

    Alternative: If MCP debug tools are unavailable, check local DB with: `sqlite3 data/heysous.db "SELECT content, category FROM memories LIMIT 20"`
  </how-to-verify>
  <verify>Human confirms memories exist in prod DB for onboarded users</verify>
  <done>Production database contains memories saved during onboarding conversations, confirming end-to-end integration</done>
  <resume-signal>Type "confirmed" if memories exist in prod, or describe what you found if not</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run tests/onboarding/memory-integration.test.ts` passes
- Production database has memories from onboarded users (verified via checkpoint)
</verification>

<success_criteria>
- Regression test proves memory tools are available and referenced in onboarding prompt
- Production data confirms memories are actually being saved during onboarding conversations
- If memories are NOT being saved in prod, a follow-up fix task is identified (but code audit suggests this is unlikely)
</success_criteria>

<output>
After completion, create `.planning/phases/52-onboarding-memory-integration/52-01-SUMMARY.md`
</output>
