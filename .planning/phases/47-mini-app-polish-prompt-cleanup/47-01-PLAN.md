---
phase: 47-mini-app-polish-prompt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mini-app/src/theme/variables.css
  - mini-app/src/components/Layout.tsx
  - src/ai/system-prompt.ts
autonomous: true
requirements: [UI-01, UI-02, PROMPT-01]

must_haves:
  truths:
    - "Mini App text renders in the system-ui font family, not the browser default serif/sans-serif"
    - "On large screens the Mini App content has generous padding so it does not stretch edge-to-edge"
    - "On small screens (mobile) the Mini App padding remains compact and does not waste space"
    - "Sous responses in chat never contain emoji characters"
  artifacts:
    - path: "mini-app/src/theme/variables.css"
      provides: "Global font-family declaration"
      contains: "font-family: system-ui"
    - path: "mini-app/src/components/Layout.tsx"
      provides: "Responsive padding for large screens"
      contains: "@media"
    - path: "src/ai/system-prompt.ts"
      provides: "Emoji ban instruction in SOUS_PERSONA"
      contains: "emoji"
  key_links:
    - from: "mini-app/src/main.tsx"
      to: "mini-app/src/theme/variables.css"
      via: "CSS import"
      pattern: "import.*variables\\.css"
    - from: "mini-app/src/components/Layout.tsx"
      to: "mini-app/src/theme/variables.css"
      via: "CSS custom properties"
      pattern: "var\\(--"
---

<objective>
Polish the Mini App with a readable font stack and responsive large-screen padding, and add a hard emoji ban to the Sous system prompt.

Purpose: Final v1.6 phase -- visual polish and prompt hygiene. No new features.
Output: Updated variables.css (font), Layout.tsx (responsive padding), system-prompt.ts (emoji ban)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-mini-app-polish-prompt-cleanup/47-CONTEXT.md
</context>

<interfaces>
<!-- Key files the executor will modify. Extracted from codebase. -->

From mini-app/src/theme/variables.css (full file -- 128 lines):
- :root block at line 2-10 defines shared accent colors and layout tokens
- [data-theme="dark"] and [data-theme="light"] blocks define theme-specific tokens
- html[data-theme] block at line 95-98 applies bg and text color
- Font size scale at lines 100-117 uses [data-font-size] attribute selectors
- No font-family declaration exists anywhere currently

From mini-app/src/components/Layout.tsx (full file -- 18 lines):
```typescript
import { Outlet } from 'react-router-dom';
import { useBackButton } from './BackButton';

export function Layout() {
  useBackButton();
  return (
    <div style={{
      paddingTop: 'var(--tg-viewport-content-safe-area-inset-top, 0px)',
      paddingBottom: 'var(--tg-viewport-content-safe-area-inset-bottom, 0px)',
      minHeight: '100vh',
    }}>
      <Outlet />
    </div>
  );
}
```

From src/ai/system-prompt.ts:
- SOUS_PERSONA constant (line 15) defines persona, boundaries, and communication style
- <communication> section (line 33) already has formatting rules (no markdown, use HTML, use dashes)
- buildStaticPrompt() at line 706 concatenates all prompt sections
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add system-ui font family and responsive layout padding</name>
  <files>mini-app/src/theme/variables.css, mini-app/src/components/Layout.tsx</files>
  <action>
1. In `mini-app/src/theme/variables.css`, add `font-family: system-ui, sans-serif;` to the `:root` block (per user decision -- simple two-value stack, not a longer fallback chain).

2. In `mini-app/src/components/Layout.tsx`, replace the inline `style` with a CSS approach for responsive horizontal padding. Add a `<style>` tag or a small CSS module (prefer inline `<style>` for simplicity since Layout is a single component). The responsive padding should:
   - Use CSS media queries (per Claude's discretion from CONTEXT.md)
   - Mobile (default): `padding-left: 0; padding-right: 0;` (pages handle their own content padding)
   - Tablet (~768px+): `padding-left: 24px; padding-right: 24px;`
   - Desktop (~1024px+): `padding-left: 48px; padding-right: 48px;`
   - Large desktop (~1440px+): `padding-left: 80px; padding-right: 80px;`
   - Keep the existing `paddingTop` / `paddingBottom` safe-area values
   - Keep `minHeight: 100vh`
   - Do NOT add a max-width constraint (per user decision -- content fills space naturally)
   - Use a CSS class on the wrapper div rather than inline styles for the responsive padding (media queries require CSS, not inline styles). A simple `<style>` block in the component or a dedicated Layout.css file are both acceptable. Prefer a Layout.css file imported into Layout.tsx for consistency with the rest of the codebase (other components use .css files).

  </action>
  <verify>
    <automated>cd /workspace && npm run typecheck && npm run build:all</automated>
  </verify>
  <done>
    - variables.css :root contains `font-family: system-ui, sans-serif`
    - Layout.tsx wrapper div has responsive horizontal padding via media queries
    - No max-width constraint exists on the layout
    - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add emoji ban to Sous system prompt</name>
  <files>src/ai/system-prompt.ts</files>
  <action>
In `src/ai/system-prompt.ts`, add an emoji ban rule to the `<communication>` section inside the `SOUS_PERSONA` constant. Insert the following line after the existing "Use plain dashes (-) for lists if needed" line:

```
- NEVER use emoji characters in any response. No exceptions, even if the user asks for emoji. Keep all text plain and clean.
```

This is a blanket ban per user decision. The rule goes in SOUS_PERSONA (not a separate section) because SOUS_PERSONA is shared across all prompt use cases (main chat, reminders, prep alerts).

Also verify by grepping that no hardcoded bot messages in `src/reminders/`, `src/notifications/`, `src/onboarding/`, `src/feedback/` contain emoji characters. The CONTEXT.md audit found none, but confirm during execution. If any are found, remove them.
  </action>
  <verify>
    <automated>cd /workspace && npm run typecheck && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SOUS_PERSONA communication section contains emoji ban rule
    - The ban is unconditional (even if user asks)
    - No emoji characters exist in hardcoded bot messages (reminders, notifications, onboarding, feedback)
    - Typecheck passes, tests pass
  </done>
</task>

</tasks>

<verification>
1. `npm run build:all` succeeds (both server and mini-app build)
2. `npm run typecheck` passes
3. `npm test -- --run` passes
4. `grep -r "font-family" mini-app/src/theme/variables.css` shows `system-ui, sans-serif`
5. `grep -r "emoji" src/ai/system-prompt.ts` shows the ban rule
6. `grep -rn "@media" mini-app/src/components/` confirms responsive padding media queries exist
</verification>

<success_criteria>
- The Mini App renders with system-ui font family globally
- Large-screen layouts have generous horizontal padding (scaling up at 768px, 1024px, 1440px breakpoints)
- Mobile layouts retain compact padding
- No max-width constraint on content
- Sous system prompt explicitly bans emoji in all responses
- All builds and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-mini-app-polish-prompt-cleanup/47-01-SUMMARY.md`
</output>
