---
phase: 53-onboarding-help-message-and-next-steps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/onboarding/state.ts
  - src/onboarding/prompt.ts
  - tests/onboarding/state.test.ts
autonomous: true
requirements: [ONBOARD-HELP]

must_haves:
  truths:
    - "The 'recipes' onboarding state is removed from the state machine"
    - "tour completion transitions directly to 'complete' (not 'recipes')"
    - "tour_only completion still transitions to 'complete'"
    - "The tour prompt is rewritten as a help/next-steps message with actionable suggestions"
    - "The tour prompt explicitly mentions /help command and Mini App menu button"
    - "buildRecipesPrompt is deleted"
    - "The marker __ONBOARDING_PHASE_COMPLETE:tour__ is still emitted for both tour and tour_only"
  artifacts:
    - path: "src/onboarding/state.ts"
      provides: "Simplified state machine without recipes state"
      contains: "getNextOnboardingState"
    - path: "src/onboarding/prompt.ts"
      provides: "Rewritten tour prompt as help/next-steps message"
      contains: "buildTourPrompt"
    - path: "tests/onboarding/state.test.ts"
      provides: "Tests for state machine transitions"
      contains: "getNextOnboardingState"
  key_links:
    - from: "src/onboarding/state.ts"
      to: "src/pipeline/processor.ts"
      via: "extractOnboardingMarker + getNextOnboardingState imports"
      pattern: "getNextOnboardingState"
    - from: "src/onboarding/prompt.ts"
      to: "src/ai/system-prompt.ts"
      via: "buildOnboardingPrompt import"
      pattern: "buildOnboardingPrompt"
---

<objective>
Remove the "recipes" onboarding phase, rewrite the tour message as a help/next-steps guide, and improve marker emission reliability.

Purpose: Users currently get pushed into a recipe-collection phase after the tour, which feels forced. Instead, end onboarding with an actionable help message that shows users what Sous can do and lets them jump right in.

Output: Simplified 3-state onboarding flow (preferences -> tour -> complete) with an improved tour/help message.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/onboarding/state.ts
@src/onboarding/prompt.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/onboarding/state.ts:
```typescript
export type OnboardingState = "preferences" | "tour" | "recipes" | "tour_only" | "complete";
export const ONBOARDING_STATES: readonly OnboardingState[] = ["preferences", "tour", "recipes", "tour_only", "complete"];
export function getNextOnboardingState(current: OnboardingState, completedPhase: string): OnboardingState;
export function extractOnboardingMarker(text: string): { text: string; completedPhase: string | null };
```

From src/onboarding/prompt.ts:
```typescript
export function buildOnboardingPrompt(state: OnboardingState): string;
// Internal: buildPreferencesPrompt(), buildTourPrompt(), buildRecipesPrompt(), buildTourOnlyPrompt()
```

From src/pipeline/processor.ts (consumer, do NOT modify):
```typescript
// Line 653: const { text: cleanText, completedPhase } = extractOnboardingMarker(response.text);
// Line 658: const nextState = getNextOnboardingState(ctx.user.onboardingState, completedPhase);
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove recipes state and fix tour->complete transition</name>
  <files>src/onboarding/state.ts, tests/onboarding/state.test.ts</files>
  <behavior>
    - getNextOnboardingState("tour", "tour") returns "complete" (was "recipes")
    - getNextOnboardingState("tour_only", "tour") returns "complete" (unchanged)
    - getNextOnboardingState("preferences", "preferences") returns "tour" (unchanged)
    - getNextOnboardingState("preferences", "skip") returns "complete" (unchanged)
    - "recipes" no longer exists in OnboardingState type or ONBOARDING_STATES array
    - extractOnboardingMarker("hello __ONBOARDING_PHASE_COMPLETE:tour__") extracts "tour" (unchanged)
    - extractOnboardingMarker("hello __ONBOARDING_PHASE_COMPLETE:recipes__") extracts "recipes" but getNextOnboardingState returns current state unchanged (dead code path, harmless)
  </behavior>
  <action>
    1. Create tests/onboarding/state.test.ts with tests for the behaviors above (RED phase).
    2. Update src/onboarding/state.ts:
       - Remove "recipes" from OnboardingState type union
       - Remove "recipes" from ONBOARDING_STATES array
       - In getNextOnboardingState: change case "tour" to always return "complete" (remove the ternary that checked for "tour_only" vs returning "recipes"). Both "tour" and "tour_only" current states should return "complete" when completedPhase is "tour".
       - Remove the case "recipes" entirely
    3. Run tests (GREEN phase).
    4. Verify no TypeScript errors with typecheck.
  </action>
  <verify>
    <automated>cd /workspace && npm test -- --run tests/onboarding/state.test.ts && npm run typecheck</automated>
  </verify>
  <done>
    - "recipes" state removed from type and array
    - tour->complete transition works for both "tour" and "tour_only" states
    - All tests pass, typecheck clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite tour prompt as help/next-steps message and delete recipes prompt</name>
  <files>src/onboarding/prompt.ts</files>
  <action>
    1. Delete buildRecipesPrompt() function entirely.
    2. Remove the case "recipes" from the switch in buildOnboardingPrompt(). Since "recipes" is no longer in the OnboardingState type, this case should already be a compile error after Task 1.
    3. Rewrite buildTourPrompt() to be a help/next-steps message. The prompt should instruct Claude to:
       - Send a brief, enthusiastic message (NOT a manual) in the tone of "here's what I can help with"
       - Mention key capabilities: meal planning across all meals, recipe management (save from links, photos, or just describe them), grocery lists, prep reminders
       - Mention the /help command: "You can always type /help to see what I can do"
       - Mention the Mini App: "Tap the menu button to open the app for grocery lists, recipes, and meal plans"
       - End with 2-3 actionable suggestions the user can try right now:
         - "Plan my dinners this week"
         - "Save this recipe: [paste a URL]"
         - "What should I make tonight?"
       - Tone guidance: "Just text me like you're texting a friend"
       - CRITICAL for marker reliability: Add explicit instruction "You MUST end your message with __ONBOARDING_PHASE_COMPLETE:tour__ on its own line. This is a hidden marker the user will never see. Do not skip it."
    4. Apply the same rewrite to buildTourOnlyPrompt() -- same help message content, same marker instruction, but keep the context note that this user joined an existing household and does not need preference questions.
    5. In the preferences prompt (buildPreferencesPrompt), update the transition text: change "naturally transition to showing them what you can do" to "naturally wrap up and let them know you're ready to help" -- since the tour/help message is now the explicit next step, the preferences phase should NOT try to also give a tour.

    All prompts use HTML parse mode for any formatting. No emoji in prompt text. No markdown.
  </action>
  <verify>
    <automated>cd /workspace && npm run typecheck && npm test -- --run</automated>
  </verify>
  <done>
    - buildRecipesPrompt() deleted
    - buildTourPrompt() rewritten as help/next-steps with actionable suggestions, /help mention, Mini App mention
    - buildTourOnlyPrompt() updated with same help content
    - Marker emission instruction is explicit and emphatic in both tour prompts
    - No "recipes" case in buildOnboardingPrompt switch
    - Typecheck passes, all tests pass
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` -- no type errors (confirms "recipes" state fully removed)
2. `npm test -- --run` -- all tests pass including new state machine tests
3. Grep confirms no remaining references to buildRecipesPrompt: `grep -r "buildRecipesPrompt\|recipes.*prompt" src/`
4. Grep confirms "recipes" removed from ONBOARDING_STATES: `grep "recipes" src/onboarding/state.ts` should return nothing
</verification>

<success_criteria>
- The onboarding state machine has 4 states: preferences, tour, tour_only, complete (no recipes)
- tour completion always transitions to complete regardless of current state
- The tour prompt reads as a friendly help message with specific actionable suggestions
- /help command and Mini App menu button are explicitly mentioned in the tour message
- Marker emission instruction is emphatic to prevent Claude from skipping it
- All tests pass, typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/53-onboarding-help-message-and-next-steps/53-01-SUMMARY.md`
</output>
