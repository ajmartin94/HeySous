---
phase: 16-household-data-migration
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/bot/handlers/message.ts
  - src/bot/handlers/grocery.ts
  - src/bot/handlers/plan.ts
  - src/bot/handlers/reminders.ts
  - src/bot/handlers/preferences.ts
  - src/bot/handlers/debug.ts
  - src/bot/handlers/feedback.ts
  - src/bot/handlers/costs.ts
  - src/pipeline/processor.ts
  - src/ai/tool-handler.ts
  - src/ai/system-prompt.ts
  - src/reminders/sender.ts
  - src/reminders/generator.ts
  - src/feedback/sender.ts
  - src/feedback/generator.ts
  - src/mini-app/auth-middleware.ts
  - src/mini-app/routes/grocery.ts
  - src/mini-app/routes/meal-plan.ts
  - src/mini-app/routes/recipes.ts
  - src/mini-app/routes/summary.ts
  - src/main.ts
autonomous: true

must_haves:
  truths:
    - "Bot handlers use ctx.householdId for all data operations"
    - "Pipeline processor uses householdId for data queries and chatId for conversation/Telegram ops"
    - "Tool handler passes householdId to all repository and retrieval calls"
    - "Reminder and feedback senders deliver to ALL household members, not just one chat"
    - "System prompt includes the current user's first name naturally"
    - "Mini-app auth resolves householdId from user record, not from Telegram ID directly"
    - "Mini-app routes use householdId for all data queries"
    - "Message queue debounce key remains per-Telegram-chat (not householdId)"
    - "Grocery list editMessageText uses Telegram chatId, not householdId"
    - "Application compiles with zero TypeScript errors"
  artifacts:
    - path: "src/pipeline/processor.ts"
      provides: "Split ID usage: householdId for data, chatId for Telegram"
      contains: "householdId"
    - path: "src/ai/system-prompt.ts"
      provides: "User name injection in system prompt"
      contains: "userName"
    - path: "src/reminders/sender.ts"
      provides: "Fan-out delivery to all household members"
      contains: "getHouseholdMembers"
    - path: "src/mini-app/auth-middleware.ts"
      provides: "HouseholdId resolution from user lookup"
      contains: "householdId"
  key_links:
    - from: "src/bot/handlers/message.ts"
      to: "src/pipeline/message-queue.ts"
      via: "chatId for debounce, ctx carries householdId"
      pattern: "ctx\\.householdId"
    - from: "src/pipeline/processor.ts"
      to: "src/ai/tool-handler.ts"
      via: "householdId passed to tool handler"
      pattern: "householdId.*ctx\\.householdId"
    - from: "src/reminders/sender.ts"
      to: "src/users/repository.ts"
      via: "getHouseholdMembers for fan-out"
      pattern: "getHouseholdMembers"
    - from: "src/mini-app/auth-middleware.ts"
      to: "src/users/repository.ts"
      via: "getUserByTelegramId for householdId lookup"
      pattern: "getUserByTelegramId"
    - from: "src/ai/system-prompt.ts"
      to: "src/pipeline/processor.ts"
      via: "userName parameter passed through"
      pattern: "buildSystemPrompt.*userName"
---

<objective>
Update all handler, pipeline, delivery, and mini-app code to use householdId for data operations while preserving chatId for Telegram operations. Add user name to system prompt. Add multi-member delivery for reminders and feedback.

Purpose: Complete the chatId-to-householdId migration by updating every caller of the data layer (updated in Plan 01). After this plan, the application compiles, runs correctly, and household members share all data. Reminders and feedback check-ins are sent to every household member. Claude knows the user's name.

Output: Fully working application with household-based data sharing, multi-member delivery, and personalized system prompt.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-household-data-migration/16-RESEARCH.md
@.planning/phases/16-household-data-migration/16-CONTEXT.md
@.planning/phases/16-household-data-migration/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update handlers, pipeline, tool-handler, and main.ts</name>
  <files>
    src/bot/handlers/message.ts
    src/bot/handlers/grocery.ts
    src/bot/handlers/plan.ts
    src/bot/handlers/reminders.ts
    src/bot/handlers/preferences.ts
    src/bot/handlers/debug.ts
    src/bot/handlers/feedback.ts
    src/bot/handlers/costs.ts
    src/pipeline/processor.ts
    src/ai/tool-handler.ts
    src/main.ts
  </files>
  <action>
**Critical concept: Two IDs in every handler.**
After this migration, handlers have TWO identifiers available:
- `ctx.householdId` (from access gate) -- use for ALL data operations (knowledge, plans, grocery, reminders, etc.)
- `String(ctx.chat.id)` -- use ONLY for Telegram API operations (sendMessage, editMessage) and per-user conversation (message queue debounce, messages table)

**Bot handler updates (7 files):**

For each handler, read the file, find where `chatId = String(ctx.chat.id)` is extracted, and change the data operations to use `ctx.householdId!` instead. The `!` non-null assertion is safe because the access gate guarantees householdId is set for all handlers (it blocks unregistered users).

- `src/bot/handlers/message.ts`: The message handler enqueues with `chatId` for debounce. **Keep chatId for the queue.enqueue() call** (debounce is per-Telegram-chat). The processor will extract `ctx.householdId` from the batch's ctx for data operations. No change needed to this file's enqueue call, but verify the chatId source is `String(ctx.chat.id)` which is correct for debounce.

- `src/bot/handlers/grocery.ts`: Replace `const chatId = String(ctx.chat.id)` with `const householdId = ctx.householdId!`. Pass `householdId` to all repository calls: `getActiveList(householdId)`, `createList(householdId)`, `completeList(householdId)`. IMPORTANT: The `ctx.api.editMessageText(chatId, messageId, ...)` call (if present) must still use `String(ctx.chat.id)` because it's a Telegram API call. If the file has editMessageText, keep a separate `const chatId = String(ctx.chat.id)` for that.

- `src/bot/handlers/plan.ts`: Replace `chatId` extraction with `ctx.householdId!` for all plan repository calls (`getActivePlans`, `getPlan`, `savePlan`) and history calls (`autoMarkCookedMeals`, `getCookingHistory`).

- `src/bot/handlers/reminders.ts`: Replace with `ctx.householdId!` for `getOrCreateSettings`, `upsertSettings`.

- `src/bot/handlers/preferences.ts`: Replace with `ctx.householdId!` for `getPreferenceSummaries`.

- `src/bot/handlers/debug.ts`: Replace with `ctx.householdId!` for data operations. If debug handler shows chatId in output, use `String(ctx.chat.id)` for display but `ctx.householdId!` for data queries.

- `src/bot/handlers/feedback.ts`: Replace with `ctx.householdId!` for `feedbackRepository` and `knowledgeRepository` calls.

- `src/bot/handlers/costs.ts`: Read this file first. If it queries `tokenUsage` by chatId, replace with `ctx.householdId!` (token_usage column is now household_id). If it shows global totals without filtering, it may need no change.

**Pipeline processor update (`src/pipeline/processor.ts`):**

This is the most critical file. The processor receives a `PendingBatch` with `batch.chatId` (Telegram chat ID for debounce/conversation) and `batch.ctx` (which has `ctx.householdId`).

Changes:
1. Extract householdId from ctx: `const householdId = (ctx as BotContext).householdId!;`
2. Keep using `chatId` (from batch) for:
   - `db.insert(messages).values({ chatId, ... })` -- messages table keeps chat_id
   - `eq(messages.chatId, chatId)` -- conversation history load is per-user
   - `ctx.api.editMessageText(chatId, ...)` -- Telegram API call
   - Log fields (chatId for Telegram context)
3. Switch to `householdId` for:
   - `createToolHandler({ ..., householdId })` instead of `chatId` (rename the dep)
   - `autoMarkCookedMeals(deps.sqlite, householdId, ...)`
   - `planRepository.getActivePlans(householdId)`
   - `getCookingHistory(deps.sqlite, householdId, ...)`
   - `buildGroceryContext(deps.sqlite, householdId)`
   - `buildReminderContext(deps.sqlite, householdId, ...)`
   - `buildFeedbackContext(deps.sqlite, householdId, ...)`
   - `getPreferenceSummaries(deps.sqlite, householdId)`
   - `db.insert(tokenUsage).values({ householdId, ... })` -- token_usage column is now household_id
4. Grocery list editMessageText (around line 268): Keep using `chatId` for `ctx.api.editMessageText(chatId, ...)` and for `groceryRepository.getActiveList(householdId)` -- the list is household-scoped but the message edit is per-Telegram-chat.
5. Add userName extraction for system prompt: `const userName = (ctx as BotContext).user?.displayName;` (the access gate sets ctx.user with the full User record which has displayName from Telegram profile). Pass it to `buildSystemPrompt`.
6. Update `ProcessorDeps.generateRemindersFn` type from `(chatId: string) => void` to `(householdId: string) => void`.

**Tool handler update (`src/ai/tool-handler.ts`):**

1. Rename the `chatId` parameter in deps to `householdId`: `deps.chatId` -> `deps.householdId`
2. Destructure as `householdId` instead of `chatId`
3. Replace ALL internal uses of `chatId` with `householdId`:
   - `retrievalService.search(householdId, query, limit)`
   - `retrievalService.getItem(id, householdId)`
   - `knowledgeRepository.create(householdId, { ... })`
   - `knowledgeRepository.getById(id, householdId)`
   - `knowledgeRepository.update(id, householdId, changes)`
   - `knowledgeRepository.delete(id, householdId)`
   - `db.insert(knowledgeChangelog).values({ householdId, ... })` (Drizzle field is now householdId)
   - `planRepository.savePlan(householdId, ...)`
   - `planRepository.getPlan(householdId, ...)`
   - `logMeal(sqlite, { householdId, ... })` (the logMeal input object field was renamed in Plan 01)
   - `getCookingHistory(sqlite, householdId, ...)`
   - `groceryRepository.createList(householdId)`
   - `groceryRepository.getActiveList(householdId)`
   - `groceryRepository.completeList(householdId)` (if used)
   - `reminderRepository.getOrCreateSettings(householdId)`
   - `reminderRepository.upsertSettings(householdId, updates)`
   - `generateRemindersFn(householdId)` (renamed param)
   - `feedbackRepository` calls (rename chatId to householdId)

**Main.ts wiring update (`src/main.ts`):**

1. The `regenerateReminders` helper (around line 112): Rename parameter from `chatId` to `householdId`. Pass `householdId` to:
   - `reminderRepository.getOrCreateSettings(householdId)`
   - `generateReminders({ ..., householdId, ... })`
   - `generateFeedbackCheckins({ ..., householdId, ... })`
2. The startup regeneration loop (around line 208): `activeSettings` returns objects with `householdId` (renamed from `chatId` in Plan 01). Update: `regenerateReminders(settings.householdId)`.
3. If any handler factory calls pass `chatId`, verify they don't need updating (most take sqlite, not chatId).
  </action>
  <verify>
Run `npx tsc --noEmit 2>&1 | head -80`. Expected: significantly fewer errors than before this task. Remaining errors should only be in sender files (updated in Task 2) and mini-app files (updated in Task 2).

Verify key patterns:
- `grep -n "ctx.householdId" src/bot/handlers/grocery.ts src/bot/handlers/plan.ts src/bot/handlers/reminders.ts` should show householdId usage.
- `grep -n "String(ctx.chat.id)" src/bot/handlers/message.ts` should still exist (debounce key).
- `grep -n "householdId" src/pipeline/processor.ts` should show householdId for data operations.
- `grep -n "chatId" src/pipeline/processor.ts` should show chatId only for messages table and Telegram API.
  </verify>
  <done>
All bot handlers use ctx.householdId for data operations. Pipeline processor correctly splits householdId (data) from chatId (conversation/Telegram). Tool handler uses householdId for all repository/retrieval calls. Main.ts wiring uses householdId for reminder regeneration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update senders, mini-app, system prompt, and generators</name>
  <files>
    src/ai/system-prompt.ts
    src/reminders/sender.ts
    src/reminders/generator.ts
    src/feedback/sender.ts
    src/feedback/generator.ts
    src/mini-app/auth-middleware.ts
    src/mini-app/routes/grocery.ts
    src/mini-app/routes/meal-plan.ts
    src/mini-app/routes/recipes.ts
    src/mini-app/routes/summary.ts
  </files>
  <action>
**System prompt update (`src/ai/system-prompt.ts`):**

Per locked decision: "System prompt includes the current user's first name (from Telegram profile) -- that's it. No household size hints or household context in system prompt. Claude addresses the user by name naturally, treats all shared data as 'yours'."

1. Add a `userName?: string` parameter to `buildSystemPrompt()` after the existing parameters.
2. If `userName` is provided, add a line to the personality section: `\nThe user's name is ${userName}. Address them by name naturally when it feels right -- don't force it into every message.`
3. Do NOT add any household references, "we" framing, or household context. Per the locked decision, household is invisible in the prompt.

**Reminder sender fan-out (`src/reminders/sender.ts`):**

Currently sends to `reminder.chatId` (which after migration is `reminder.householdId` -- a household ID, NOT a valid Telegram chat ID). Must resolve to actual Telegram user IDs.

1. Add `sqlite: BetterSqlite3.Database` to `ReminderSenderDeps` interface.
2. Import `getHouseholdMembers` from `../users/repository.js`.
3. In `sendReminder()`, after generating the text:
   - Replace the single `bot.api.sendMessage(reminder.chatId, ...)` with a fan-out loop:
   ```typescript
   const members = getHouseholdMembers(sqlite, reminder.householdId);
   let sent = false;
   for (const member of members) {
     try {
       await bot.api.sendMessage(member.telegramId, text, { parse_mode: "HTML" });
       sent = true;
     } catch (error: unknown) {
       const err = error as { error_code?: number };
       if (err.error_code === 403) {
         logger.warn({ reminderId: reminder.id, telegramId: member.telegramId }, "Bot blocked by user, skipping");
       } else {
         logger.error({ reminderId: reminder.id, telegramId: member.telegramId, error }, "Failed to send reminder to member");
       }
     }
   }
   return sent;
   ```
4. Update the `retrievalService.getItem()` call: it takes `(id, householdId)` -- use `reminder.householdId` (the field was renamed from `chatId` in Plan 01's type update).
5. Update all references to `reminder.chatId` -> `reminder.householdId` throughout the file (the Reminder type field was renamed in Plan 01).
6. Update log fields from `chatId: reminder.chatId` to `householdId: reminder.householdId`.

**Feedback sender fan-out (`src/feedback/sender.ts`):**

Same pattern as reminder sender:

1. Add `sqlite: BetterSqlite3.Database` to `FeedbackSenderDeps` interface.
2. Import `getHouseholdMembers` from `../users/repository.js`.
3. In `sendCheckin()`, replace the single `bot.api.sendMessage(reminder.chatId, ...)` with a fan-out loop identical to the reminder sender pattern.
4. Update all `reminder.chatId` references to `reminder.householdId`.
5. Update log fields similarly.

**Reminder generator (`src/reminders/generator.ts`):**

Rename the `chatId` parameter in the deps object to `householdId`. Update the internal call to `reminderRepository.deleteFutureReminders(householdId)` and `planRepository.getActivePlans(householdId)`. Update any reminder creation calls that pass chatId to pass householdId.

**Feedback generator (`src/feedback/generator.ts`):**

Same pattern: rename `chatId` to `householdId` in deps and all internal calls.

**Main.ts sender wiring:**

Update the `createReminderSender` and `createFeedbackSender` calls in `src/main.ts` to pass `sqlite` in the deps:
- `createReminderSender({ bot, claudeClient, retrievalService, logger, sqlite })`
- `createFeedbackSender({ bot, logger, sqlite })`

**Mini-app auth middleware (`src/mini-app/auth-middleware.ts`):**

1. Add `sqlite: BetterSqlite3.Database` parameter to `validateInitData` (change to a factory function that takes sqlite and returns the middleware, following the access-gate pattern).

   Approach: Change the export to a factory:
   ```typescript
   export function createInitDataValidator(sqlite: BetterSqlite3.Database) {
     return function validateInitData(req: Request, res: Response, next: NextFunction): void {
       // existing validation logic...
       // After extracting userId:
       const user = getUserByTelegramId(sqlite, String(userId));
       if (!user) {
         res.status(401).json({ error: "User not registered" });
         return;
       }
       res.locals.householdId = user.householdId;
       res.locals.chatId = String(userId); // keep for backward compat if needed
       next();
     };
   }
   ```
2. Import `getUserByTelegramId` from `../users/repository.js`.
3. Import `BetterSqlite3` type.

**Mini-app router update:**

Check `src/mini-app/router.ts` to see how the auth middleware is mounted and update the call to pass sqlite:
- Change from `validateInitData` to `createInitDataValidator(sqlite)`.

**Mini-app route updates (4 files):**

For each route file, change `res.locals.chatId` to `res.locals.householdId`:

- `src/mini-app/routes/grocery.ts`: Change all `res.locals.chatId as string` to `res.locals.householdId as string`. The `repo.getActiveList(chatId)` becomes `repo.getActiveList(householdId)`, etc.
- `src/mini-app/routes/meal-plan.ts`: Same pattern -- replace chatId with householdId for data queries.
- `src/mini-app/routes/recipes.ts`: Same pattern.
- `src/mini-app/routes/summary.ts`: Same pattern.
  </action>
  <verify>
Run `npx tsc --noEmit`. Expected: ZERO compilation errors. The entire application should compile cleanly.

If there are errors, fix them -- they likely indicate a missed chatId reference somewhere. Common places to check:
- Drizzle `.values({ chatId })` shorthand that should be `.values({ householdId })`
- Type mismatches where a field was renamed but a consumer wasn't updated
- Import paths for the new factory function

Run a final comprehensive check:
- `grep -rn "\.chatId" src/ --include="*.ts" | grep -v node_modules | grep -v "messages\." | grep -v "conversation" | grep -v "\.d\.ts"` -- should only show chatId in: messages table usage, conversation types, message queue (debounce), and PendingBatch. Any hits in data-layer files indicate missed renames.
  </verify>
  <done>
System prompt includes userName parameter and injects user's first name naturally. Reminder sender resolves householdId to all member telegramIds and sends to each. Feedback sender does the same. Mini-app auth middleware resolves householdId from user record. All mini-app routes use householdId. Application compiles with zero TypeScript errors. The full chatId-to-householdId migration is complete.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -rn "String(ctx.chat.id)" src/bot/handlers/` should only appear in message.ts (for debounce key)
3. `grep -rn "ctx.householdId" src/bot/handlers/` should appear in all data-accessing handlers
4. `grep -n "getHouseholdMembers" src/reminders/sender.ts src/feedback/sender.ts` confirms fan-out
5. `grep -n "userName" src/ai/system-prompt.ts` confirms user name injection
6. `grep -n "householdId" src/mini-app/auth-middleware.ts` confirms middleware resolution
7. `grep -n "householdId" src/pipeline/processor.ts` confirms split ID usage
8. Message queue debounce key remains per-Telegram-chat
9. Messages table insert still uses chatId
10. Grocery list editMessageText still uses Telegram chatId
</verification>

<success_criteria>
- Application compiles cleanly with `npx tsc --noEmit`
- All bot handlers use ctx.householdId for data, String(ctx.chat.id) only for Telegram ops
- Pipeline processor correctly splits chatId (conversation/Telegram) from householdId (data)
- Tool handler passes householdId to every repository and retrieval call
- Reminder sender sends to ALL household members via getHouseholdMembers fan-out
- Feedback sender sends to ALL household members via same pattern
- System prompt includes user's first name per locked decision ("just natural 'you' conversation")
- System prompt has NO household references, no "we" framing, no household size hints
- Mini-app auth middleware resolves householdId from user record
- All 4 mini-app routes use householdId for data queries
- Message queue debounce stays per-Telegram-chat
- Messages table inserts and queries use Telegram chatId
</success_criteria>

<output>
After completion, create `.planning/phases/16-household-data-migration/16-02-SUMMARY.md`
</output>
