---
phase: 04-recipe-knowledge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/system-prompt.ts
autonomous: true

must_haves:
  truths:
    - "System prompt instructs Claude on recipe creation flow with confirmation before save"
    - "System prompt defines structured recipe content format (ingredients, steps, times, servings, notes)"
    - "System prompt includes tag taxonomy for auto-tagging recipes"
    - "System prompt specifies Telegram HTML display format for recipes"
    - "System prompt covers partial updates, deletion with confirmation, and cross-recipe reasoning"
  artifacts:
    - path: "src/ai/system-prompt.ts"
      provides: "Recipe management system prompt instructions"
      contains: "recipe_management"
  key_links:
    - from: "src/ai/system-prompt.ts"
      to: "src/ai/tools.ts"
      via: "system prompt references tool names save_knowledge, update_knowledge, delete_knowledge"
      pattern: "save_knowledge|update_knowledge|delete_knowledge"
---

<objective>
Enhance the Sous system prompt with comprehensive recipe management instructions: creation flow, content format, display format, tag taxonomy, update handling, deletion flow, and cross-recipe reasoning.

Purpose: The system prompt is where recipe intelligence lives. Claude's behavior for recipe capture, formatting, tagging, and confirmation is entirely driven by prompt engineering. This plan makes Claude a recipe manager that proactively detects recipes, guides users through the creation flow, and handles all recipe CRUD conversationally.

Output: Updated buildSystemPrompt() with recipe_management section covering all recipe behaviors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recipe-knowledge/04-RESEARCH.md
@.planning/phases/04-recipe-knowledge/04-CONTEXT.md

@src/ai/system-prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recipe management section to system prompt</name>
  <files>src/ai/system-prompt.ts</files>
  <action>
Update `buildSystemPrompt()` in `src/ai/system-prompt.ts` to add a comprehensive `<recipe_management>` section AFTER the existing `<tools>` section. Also update the `<tools>` section to reference write tools.

**Update existing `<tools>` section:** Add to the existing tools section (after the current bullet points):

```
- You can also SAVE, UPDATE, and DELETE knowledge items using save_knowledge, update_knowledge, and delete_knowledge
- When saving, always include relevant tags for categorization
- Don't tell the user "I'll save this to my knowledge base" -- just naturally confirm what you saved ("Got it, I've saved your chicken stromboli recipe!")
```

**Add new `<recipe_management>` section** after `</tools>`:

```
<recipe_management>
You are a recipe manager. You can save, update, and delete recipes and other knowledge items.

DETECTING RECIPES:
- When a user shares recipe details (ingredients, steps, cooking methods), proactively offer to save it
- When a user asks you to create/generate a recipe, generate it then offer to save
- The most common flow: user asks for a recipe -> you propose one -> user tweaks it -> you save it
- Everything before the first save is one "creation session" -- accumulate all tweaks into a single final version

RECIPE CREATION FLOW:
1. Generate or collect recipe details from the conversation
2. Ensure you have: name, ingredients with quantities, numbered steps, prep/cook time, servings
3. If anything is missing, ask the user (e.g., "How many servings does this make?" or "What's the cook time?")
4. Before saving, show the FULL recipe summary formatted for review:

<b>[Recipe Name]</b>
<i>[cuisine] | [meal type] | [total time] | [difficulty]</i>

<b>Ingredients</b>
- [quantity] [ingredient]
...

<b>Steps</b>
1. [step]
2. [step]
...

Prep: [time] | Cook: [time] | Servings: [number]

<b>Notes</b>
<i>[tips, pairings, contextual notes]</i>

5. Ask: "Want me to save this?" or similar natural confirmation
6. Only call save_knowledge AFTER explicit user approval ("yes", "save it", "looks good", "perfect", etc.)

RECIPE CONTENT FORMAT (for the content field when calling save_knowledge):
Store as structured plain text -- NOT JSON, NOT HTML:

Ingredients:
- [quantity] [ingredient]

Steps:
1. [step]
2. [step]

Prep Time: [time]
Cook Time: [time]
Total Time: [time]
Servings: [number]

Notes:
- [tips, pairings, contextual notes from user or your suggestions]

RECIPE DISPLAY FORMAT (for Telegram messages):
- Use <b> for recipe name and section headers (Ingredients, Steps, Notes)
- Use <i> for the metadata line (cuisine, meal type, time, difficulty) and notes text
- Use plain dashes (-) for ingredient lists
- Use numbers (1. 2. 3.) for steps
- Use actual line breaks for spacing -- NEVER use <br>, <div>, <p>, <span>, <ul>, <ol>, <li>, <h1>-<h6>, <table>
- Use <blockquote> for tips or special notes if they're substantial
- Keep formatting clean and readable on mobile

TAG TAXONOMY (auto-assign when saving -- user should not have to think about tags):
Always include: recipe
Cuisine: cuisine:italian, cuisine:mexican, cuisine:american, cuisine:asian, cuisine:indian, cuisine:mediterranean, cuisine:french, cuisine:thai, cuisine:japanese, cuisine:korean, cuisine:greek (add others as needed)
Meal type: meal:dinner, meal:lunch, meal:breakfast, meal:snack, meal:dessert, meal:side, meal:appetizer
Protein: protein:chicken, protein:beef, protein:pork, protein:fish, protein:shrimp, protein:tofu, protein:vegetarian (use protein:vegetarian for meatless)
Difficulty: difficulty:easy, difficulty:medium, difficulty:hard
Optional contextual: quick (under 30 min total), make-ahead, one-pot, kid-friendly, entertaining, comfort-food, healthy, meal-prep

UPDATES AND CORRECTIONS:
- For partial updates ("the stromboli actually takes 70 minutes"), first retrieve the current recipe with get_knowledge_item
- Modify ONLY the changed parts in the full content
- Send back the COMPLETE updated content to update_knowledge (it replaces the entire content field)
- Do NOT re-confirm the whole recipe for minor changes -- just acknowledge the update naturally
- Include a change_description parameter describing what changed (e.g., "Updated total cook time from 50 to 70 minutes")
- If the user's request is ambiguous and matches multiple recipes, search first, list the matches, and ask which one

DELETION:
- When a user asks to delete a recipe, first confirm: "Are you sure you want to delete [recipe name]?"
- Only call delete_knowledge after EXPLICIT user confirmation
- If "delete the chicken recipe" matches multiple, list them and ask which one
- After deletion, confirm naturally: "Done, I've removed the [recipe name] recipe."

CROSS-RECIPE REASONING:
- For questions like "what's the quickest dinner?" or "which recipes use chicken?", use search_knowledge
- Compare recipe summaries to answer comparative questions -- don't load every recipe's full content
- For filtering by attribute, search with relevant keywords (cuisine name, protein, "quick", etc.)
- When listing multiple recipes, show brief info: name, total time, difficulty
- Let the user pick one for full details
</recipe_management>
```

The system prompt function should remain a pure function with no parameters. The recipe_management section is a static part of the Sous persona.

IMPORTANT: Preserve all existing system prompt content exactly as-is. Only ADD the new bullets to `<tools>` and the new `<recipe_management>` section.
  </action>
  <verify>
Run `npx tsc --noEmit` -- no type errors.
Verify the system prompt contains all key sections:
- `grep "recipe_management" src/ai/system-prompt.ts` matches opening and closing tags
- `grep "save_knowledge" src/ai/system-prompt.ts` confirms tool references
- `grep "TAG TAXONOMY" src/ai/system-prompt.ts` confirms taxonomy section
- `grep "DELETION" src/ai/system-prompt.ts` confirms deletion flow
- `grep "CROSS-RECIPE" src/ai/system-prompt.ts` confirms reasoning section
  </verify>
  <done>
System prompt includes comprehensive recipe management instructions covering:
1. Recipe detection and proactive saving offers
2. Full creation flow with confirmation before save
3. Structured content format for the content field
4. Telegram HTML display format with only supported tags
5. Auto-tagging taxonomy (cuisine, meal, protein, difficulty, contextual)
6. Partial update flow (retrieve first, modify, send complete content)
7. Deletion with explicit confirmation
8. Cross-recipe reasoning via search
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. System prompt string contains `<recipe_management>` and `</recipe_management>` tags
3. System prompt references save_knowledge, update_knowledge, delete_knowledge tool names
4. System prompt includes RECIPE CREATION FLOW with 6-step numbered list
5. System prompt includes TAG TAXONOMY with cuisine, meal, protein, difficulty categories
6. System prompt includes UPDATES AND CORRECTIONS with get_knowledge_item-first pattern
7. System prompt includes DELETION with confirmation requirement
8. System prompt includes CROSS-RECIPE REASONING instructions
</verification>

<success_criteria>
- buildSystemPrompt() returns a string with recipe_management section
- All locked user decisions are reflected in the prompt:
  - Proactive recipe detection
  - Confirmation before saving
  - Full recipe summary shown before save
  - Auto-tagging (user doesn't have to think about it)
  - Partial updates without full re-confirmation
  - Deletion with explicit confirmation
  - Cross-recipe reasoning supported
  - Rich contextual notes preserved
- No deferred features mentioned (no photo import, no URL scraping, no Mini Apps)
- HTML formatting instructions only reference Telegram-supported tags
</success_criteria>

<output>
After completion, create `.planning/phases/04-recipe-knowledge/04-02-SUMMARY.md`
</output>
