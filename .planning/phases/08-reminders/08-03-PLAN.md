---
phase: 08-reminders
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/reminders/sender.ts
  - src/reminders/poller.ts
autonomous: true

must_haves:
  truths:
    - "Poller checks for due reminders every 60 seconds"
    - "Due reminders are sent to users via bot.api.sendMessage"
    - "Claude generates varied, persona-consistent reminder text at send time"
    - "Reminders are marked as sent BEFORE attempting delivery to prevent duplicates"
    - "Overdue reminders from before process start are processed on startup"
    - "Failed sends are caught and logged, never crash the poller"
  artifacts:
    - path: "src/reminders/sender.ts"
      provides: "Sends a single reminder via Telegram with Claude-generated text"
      exports: ["createReminderSender"]
    - path: "src/reminders/poller.ts"
      provides: "setInterval-based poller that queries and dispatches due reminders"
      exports: ["createReminderPoller"]
  key_links:
    - from: "src/reminders/poller.ts"
      to: "src/reminders/repository.ts"
      via: "queries getDueReminders on each tick"
      pattern: "getDueReminders"
    - from: "src/reminders/sender.ts"
      to: "bot.api.sendMessage"
      via: "sends proactive Telegram messages"
      pattern: "sendMessage"
    - from: "src/reminders/sender.ts"
      to: "Anthropic API"
      via: "generates reminder text with Claude"
      pattern: "claudeClient|sendMessage"
---

<objective>
Create the runtime reminder engine: a poller that checks for due reminders every 60 seconds and a sender that delivers them via Telegram with Claude-generated text.

Purpose: This is the heart of proactive messaging -- the poller runs continuously, finds reminders that are due, generates persona-consistent text via Claude, and sends them to users via bot.api.sendMessage. It's designed to be crash-safe (mark sent before delivery attempt) and restart-safe (process overdue on startup).
Output: Sender module (text generation + delivery) and poller module (interval loop + query + dispatch).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reminders/08-CONTEXT.md
@.planning/phases/08-reminders/08-RESEARCH.md
@.planning/phases/08-reminders/08-01-SUMMARY.md

# Key source files:
@src/ai/claude-client.ts
@src/config.ts
@src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reminder sender with Claude text generation</name>
  <files>src/reminders/sender.ts</files>
  <action>
Create `createReminderSender(deps)` factory function.

Dependencies:
```typescript
interface ReminderSenderDeps {
  bot: { api: { sendMessage: (chatId: string, text: string) => Promise<unknown> } };
  claudeClient: { sendMessage: (messages: string[], systemPrompt?: string) => Promise<{ text: string; usage: { inputTokens: number; outputTokens: number; cacheCreationInputTokens: number; cacheReadInputTokens: number } }> };
  logger: Logger;
}
```

Use minimal interface types for bot and claudeClient deps (not full grammY/Anthropic types) to keep the sender decoupled. Import Logger from pino.

Returns object with:

**`sendReminder(reminder: Reminder): Promise<boolean>`**

1. Parse `reminder.contextJson` to get meal context
2. Generate varied reminder text via Claude:
   - Build a focused system prompt for reminder text generation (NOT the full Sous prompt):
     ```
     You are Sous, a warm and friendly kitchen assistant. Write a brief, casual reminder message for Telegram.
     Keep it to 1-2 short sentences. Use HTML formatting (<b>, <i>) sparingly.
     Be varied -- don't use the same phrasing every time. Sound like a friendly kitchen buddy, not a calendar alarm.
     Do NOT use markdown. Do NOT use emojis excessively. Keep it natural.
     ```
   - User message describes what reminder to write based on type:
     - `morning_summary`: "Write a morning prep summary for today. Meals planned: {list of meals with types}. {any prep notes if available}. Keep it like a sticky note on the fridge -- 2-3 lines max."
     - `prep_alert`: "Write a brief prep reminder. The recipe '{recipeName}' is planned for {mealDate} ({mealType}). Remind them about advance prep needed (like defrosting, marinating). Just the heads-up, no instructions. One sentence."
     - `start_cooking`: "Write a start-cooking nudge. Tonight's dinner is '{recipeName}'. Keep it to one casual sentence."
   - Call `claudeClient.sendMessage([userMessage], reminderSystemPrompt)`
3. Send via `bot.api.sendMessage(reminder.chatId, generatedText)`
4. Return true on success, false on failure

**Error handling:**
- If Claude API fails, use plain-text fallback:
  - morning_summary: "Good morning! Here's what's on the menu today: {meal list}"
  - prep_alert: "Heads up -- {recipeName} is on tomorrow's menu. Check if anything needs advance prep!"
  - start_cooking: "Time to start cooking! Tonight: {recipeName}"
- If Telegram sendMessage fails with 403 (bot blocked), log a warning and return false. Do NOT retry.
- If Telegram sendMessage fails with other error, log error and return false.
- Wrap entire function in try/catch -- sender must NEVER throw.

Log each send attempt at info level with chatId, reminder type, and success/failure.
  </action>
  <verify>
    `npx tsc --noEmit` compiles without errors. createReminderSender is exported.
  </verify>
  <done>
    Sender generates varied, persona-consistent reminder text via Claude, delivers via Telegram, handles all error cases gracefully with fallback text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reminder poller with 60-second interval</name>
  <files>src/reminders/poller.ts</files>
  <action>
Create `createReminderPoller(deps)` factory function.

Dependencies:
```typescript
interface ReminderPollerDeps {
  reminderRepository: ReturnType<typeof createReminderRepository>;
  sender: { sendReminder: (reminder: Reminder) => Promise<boolean> };
  logger: Logger;
}
```

Import types from ./types.js and type for createReminderRepository from ./repository.js.

Returns object with:

**`start(): void`**
- Log "Reminder poller started (60s interval)" at info level
- Process any overdue reminders immediately on start (call `tick()` once)
- Set up `setInterval(tick, 60_000)` -- 60 second interval
- Store the interval reference for cleanup

**`stop(): void`**
- Clear the interval
- Log "Reminder poller stopped" at info level

**`async tick(): Promise<void>`** (can be called directly for testing)
- Query `reminderRepository.getDueReminders()` for all pending reminders where due_at <= now
- If no due reminders, return immediately (no logging to avoid noise)
- For each due reminder:
  1. **Mark as sent FIRST** (before attempting delivery) -- call `reminderRepository.markSent(reminder.id, "")` with empty text. This prevents duplicates if the process crashes during send. Per research pitfall: mark BEFORE send, not after.
  2. Call `sender.sendReminder(reminder)`
  3. If send succeeds, update with actual generated text: `reminderRepository.markSent(reminder.id, generatedText)` -- actually, markSent was already called. Instead, do a simpler approach: markSent sets status only, then update generated_text separately. **Revised approach:** Call markSent with a placeholder first. If sender returns the generated text, update it. OR simpler: just mark sent with empty string before send, the text is fire-and-forget anyway.
  4. If send fails, call `reminderRepository.markFailed(reminder.id)` -- override the premature markSent. This way, on restart, failed reminders are NOT retried (they're marked failed, not pending). Per user decision: fire and forget, no re-reminders.
  5. Log each processed reminder at info level with id, type, chatId, success/failure

- Wrap entire tick in try/catch -- poller must NEVER crash. Log errors at error level.
- Process reminders sequentially (not in parallel) to avoid overwhelming Telegram rate limits.

**Revised send flow (cleaner):**
1. Mark status as 'sent' immediately to prevent re-processing
2. Attempt send
3. If send fails, mark as 'failed' (overrides 'sent')
4. If send succeeds, optionally update generated_text (cosmetic, for debugging/logging)

Actually, simplest correct approach: change markSent to accept generatedText. In the flow:
1. Set status='sent' and generated_text='' BEFORE attempting send
2. Call sender.sendReminder(reminder) which returns boolean
3. If failed, update status to 'failed'
4. The generated text is ephemeral anyway -- no need to store it back

This keeps the repository API clean and the flow safe against duplicates.
  </action>
  <verify>
    `npx tsc --noEmit` compiles without errors. createReminderPoller is exported with start(), stop(), and tick() methods.
  </verify>
  <done>
    Poller runs every 60 seconds, queries due reminders, marks them sent before delivery to prevent duplicates, sends via sender, handles failures gracefully. Processes overdue reminders on startup. Can be cleanly stopped on shutdown.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. src/reminders/sender.ts exports createReminderSender
3. src/reminders/poller.ts exports createReminderPoller
4. Sender has Claude text generation with fallback
5. Poller has start/stop/tick lifecycle
6. Mark-sent-before-send pattern prevents duplicates
</verification>

<success_criteria>
- Poller queries due reminders on a 60-second interval
- Sender generates varied text via Claude with plain-text fallback
- Duplicate prevention via mark-before-send
- Overdue reminders processed on startup
- All errors caught and logged -- neither sender nor poller ever crash
</success_criteria>

<output>
After completion, create `.planning/phases/08-reminders/08-03-SUMMARY.md`
</output>
