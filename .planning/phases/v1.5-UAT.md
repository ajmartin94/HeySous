---
status: complete
phase: v1.5-agent-hardening-polish
source: 32-01-SUMMARY.md, 32-02-SUMMARY.md, 33-01-SUMMARY.md, 33-02-SUMMARY.md, 33-03-SUMMARY.md, 34-01-SUMMARY.md, 34-02-SUMMARY.md, 35-01-SUMMARY.md, 35-02-SUMMARY.md, 35-03-SUMMARY.md, 36-01-SUMMARY.md, 36-02-SUMMARY.md, 36-03-SUMMARY.md, 37-01-SUMMARY.md, 37-02-SUMMARY.md, 38-01-SUMMARY.md, 38-02-SUMMARY.md, 39-01-SUMMARY.md, 39-02-SUMMARY.md, 40-01-SUMMARY.md, 40-02-SUMMARY.md
started: 2026-02-24T22:00:00Z
updated: 2026-02-24T19:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Streaming Response Delivery
expected: Send a message to Sous in Telegram. The response should appear progressively — text streaming in incrementally rather than appearing all at once after a long wait.
result: pass

### 2. Tool Status Labels During Streaming
expected: Ask Sous to do something that triggers a tool call (e.g., "save this recipe: Classic Pancakes — Ingredients: flour, eggs, milk, butter. Steps: mix dry, add wet, cook on griddle"). While processing, you should see an inline status label (like a search or save indicator) before the final response appears.
result: pass

### 3. Message Length Limit
expected: Send a message longer than 4,000 characters to Sous. You should receive a friendly, in-character rejection message (Sous-style) instead of the message being processed. The oversized message should NOT appear in conversation history.
result: pass

### 4. Mini App Theme Toggle
expected: Open the Mini App and navigate to Settings (gear icon in the Hub header). You should see theme options (Light/Dark). Toggling between them should immediately change the app's appearance — backgrounds, text colors, and component styling should all update.
result: issue
reported: "no change on button click. cards on main page not changing theme, bad backgrounds in both themes"
severity: major
fix: Added all 13 --tg-theme-* variables + --tgui--section_bg_color to both themes (variables.css + ThemeContext.tsx inline overrides). Passed AppRoot appearance prop. Fixed in-session.

### 5. Mini App Font Size
expected: In Settings, you should see font size options (Small / Medium / Large). Changing the font size should visibly adjust text throughout the Mini App, including recipe content, tags, and navigation elements.
result: pass

### 6. Theme and Font Persistence
expected: After selecting a theme and font size, close the Mini App completely and reopen it. Your theme and font size preferences should be preserved — the app should open with your last selected settings, not reset to defaults.
result: pass

### 7. Tag Contrast (Dark Theme)
expected: In Dark theme, view a recipe that has tags. The tag text should be clearly readable against the tag background — no squinting required. Tags should have good contrast (green-ish text on darker background).
result: pass

### 8. Tag Contrast (Light Theme)
expected: Switch to Light theme and view the same recipe with tags. Tags should still be clearly readable — dark text on a lighter background. No washed-out or hard-to-read tags.
result: pass

### 9. Help Page Content
expected: Open the Help page from the Mini App. It should contain comprehensive documentation covering: saving recipes (URL, photo, chat), meal planning, grocery lists, reminders, preferences, feedback, and Mini App features. Each section should include "Try:" example messages. Content should be written in Sous's warm, conversational voice. Font size should respect your Settings preference.
result: pass

### 10. Admin Tab Visibility (Admin User)
expected: As an admin user, open the Mini App Hub. You should see an "Admin" navigation tab alongside the existing tabs (Recipes, Plans, etc.). Tapping it should navigate to the admin dashboard.
result: pass

### 11. Admin Dashboard — Stats and Cost Chart
expected: On the admin dashboard, you should see summary stat cards (message counts, active households, etc.) and a cost breakdown section with an SVG bar chart showing token spend with a budget reference line.
result: pass

### 12. Admin Dashboard — Activity Feed
expected: The admin dashboard should show a recent activity feed with timestamps showing messages, tool calls, and errors. It should support pagination to view older activity.
result: pass

### 13. Admin Dashboard — Feedback Overview
expected: The admin dashboard should include a feedback section showing recent feedback entries. It should include a 7-day trend comparison.
result: pass

### 14. Admin Dashboard — Time Range Filter
expected: The admin dashboard should have time range controls (today / 7d / 30d). Switching between them should update all dashboard sections with data for the selected period.
result: pass

### 15. Start-Cooking Reminder Default Timing
expected: Set a meal plan with a recipe that has no explicit prep/cook time data, and have a start-cooking reminder enabled. The reminder should fire approximately 45 minutes before dinner time, not at dinner time itself.
result: pass

### 16. Content-Aware Duplicate Detection
expected: Save a recipe, then try to save a very similar recipe (same ingredients, slightly different title). Sous should detect it as a near-duplicate and flag it, rather than silently saving a duplicate entry.
result: pass

## Summary

total: 16
passed: 15
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "Theme toggle immediately updates all Mini App components including cards and sections"
  status: fixed
  reason: "User reported: no change on button click. Cards not changing theme, bad backgrounds in both themes"
  severity: major
  test: 4
  root_cause: "Telegram SDK bindCssVars() sets --tg-theme-* as inline styles overriding CSS [data-theme] selectors. Missing --tg-theme-section-bg-color and 7 other tg-theme variables. AppRoot not receiving appearance prop."
  artifacts:
    - path: "mini-app/src/theme/ThemeContext.tsx"
      issue: "Missing inline style overrides for all 13 tg-theme variables"
    - path: "mini-app/src/theme/variables.css"
      issue: "Missing section-bg-color, header-bg-color, link-color, button-color, accent-text-color, subtitle-text-color, section-header-text-color"
    - path: "mini-app/src/App.tsx"
      issue: "AppRoot not receiving appearance prop from theme context"
  missing: []
  debug_session: ""
  fix_applied: "Added all 13 --tg-theme-* vars + --tgui--section_bg_color to both themes in variables.css and ThemeContext.tsx inline overrides. Created AppShell wrapper passing appearance to AppRoot."
