---
phase: 18-app-feedback
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/mini-app/routes/app-feedback.ts
  - src/mini-app/router.ts
  - mini-app/src/pages/Feedback.tsx
  - mini-app/src/pages/Hub.tsx
  - mini-app/src/router.tsx
autonomous: true

must_haves:
  truths:
    - "Mini App hub shows a Give Feedback card with MessageSquare icon that navigates to /feedback"
    - "Feedback page shows a text area and submit button"
    - "Submitting feedback text calls POST /api/feedback and shows a thank-you confirmation"
    - "Submitting empty text is prevented client-side and rejected server-side with 400"
    - "After submission, the form resets and shows a success state"
  artifacts:
    - path: "src/mini-app/routes/app-feedback.ts"
      provides: "POST /api/feedback endpoint handler"
      exports: ["createAppFeedbackRoutes"]
    - path: "mini-app/src/pages/Feedback.tsx"
      provides: "Feedback form page with textarea and submit"
      exports: ["Feedback"]
  key_links:
    - from: "mini-app/src/pages/Feedback.tsx"
      to: "src/mini-app/routes/app-feedback.ts"
      via: "apiFetch('/feedback', { method: 'POST', body })"
      pattern: "apiFetch.*feedback"
    - from: "mini-app/src/pages/Hub.tsx"
      to: "mini-app/src/router.tsx"
      via: "navigate('/feedback') on card click"
      pattern: "navigate.*feedback"
    - from: "src/mini-app/router.ts"
      to: "src/mini-app/routes/app-feedback.ts"
      via: "router.post('/feedback', appFeedback.submit)"
      pattern: "feedback"
---

<objective>
Add the Mini App feedback form -- a "Give Feedback" card on the Hub that opens a simple text input page, submitting feedback to the same app_feedback table via a new API endpoint.

Purpose: Completes the fourth feedback collection channel (Mini App) alongside the command, implicit, and proactive channels from Plan 01.
Output: Working feedback form in the Mini App hub with server-side API endpoint.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-app-feedback/18-01-SUMMARY.md
@src/mini-app/router.ts
@src/mini-app/routes/grocery.ts
@src/mini-app/auth-middleware.ts
@mini-app/src/pages/Hub.tsx
@mini-app/src/pages/Grocery.tsx
@mini-app/src/router.tsx
@mini-app/src/api.ts
@mini-app/src/theme/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side feedback API endpoint</name>
  <files>
    src/mini-app/routes/app-feedback.ts
    src/mini-app/router.ts
  </files>
  <action>
Create the server-side API endpoint for Mini App feedback submission.

**src/mini-app/routes/app-feedback.ts:**
- Export `createAppFeedbackRoutes(sqlite: BetterSqlite3.Database)` following the exact same factory pattern as `createGroceryRoutes` in `src/mini-app/routes/grocery.ts`
- Import `createAppFeedbackRepository` from `../../app-feedback/repository.js`
- Return an object with a single handler:
  - `submit(req: Request, res: Response)`:
    - Extract `householdId` from `res.locals.householdId as string` (set by auth middleware)
    - Extract `userId` from `res.locals.chatId as string` (set by auth middleware -- this is the Telegram user ID string)
    - Extract `text` from `req.body` as `{ text?: string }`
    - Validate: if no text or empty after trim, return `res.status(400).json({ error: "Feedback text is required" })`
    - Call `repository.saveFeedback({ householdId, userId, text: text.trim(), source: "mini-app" })`
    - Return `res.json({ ok: true })`
- Use .js import extensions

**src/mini-app/router.ts:**
- Import `createAppFeedbackRoutes` from `./routes/app-feedback.js`
- After the meal plan routes block, add:
  - `const appFeedback = createAppFeedbackRoutes(deps.sqlite);`
  - `router.post("/feedback", appFeedback.submit);`
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify:
- `src/mini-app/routes/app-feedback.ts` exports createAppFeedbackRoutes
- `src/mini-app/router.ts` wires POST /feedback route
  </verify>
  <done>
POST /api/feedback endpoint accepts { text } body, validates non-empty, saves to app_feedback with source "mini-app", returns { ok: true }. Route is wired through auth middleware (initData validation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Mini App feedback page and Hub card</name>
  <files>
    mini-app/src/pages/Feedback.tsx
    mini-app/src/pages/Hub.tsx
    mini-app/src/router.tsx
  </files>
  <action>
Create the client-side feedback form and add the Hub navigation card.

**mini-app/src/pages/Feedback.tsx:**
- Create a simple feedback form page following existing page patterns (Grocery.tsx, Recipes.tsx)
- Import: `useState` from react, `Section` from `@telegram-apps/telegram-ui`, `apiFetch` from `../api`, `colors` from `../theme/tokens`
- Component `Feedback` (named export):
  - State: `text` (string), `submitted` (boolean), `submitting` (boolean)
  - Render a page with:
    - Header: "Give Feedback" (styled like other page headers -- fontSize var(--hs-font-size-heading), fontWeight 600, color var(--hs-accent))
    - A `<textarea>` (NOT the @telegram-apps/telegram-ui Input -- use a plain HTML textarea for multi-line) with:
      - placeholder: "What's on your mind? Tell us what you love, what's frustrating, or what you wish was different..."
      - Styling: width 100%, min-height 120px, resize vertical, border-radius 12px, padding 12px, font-size 16px, border 1px solid var(--tgui--secondary_hint_color), background var(--tgui--bg_color), color var(--tgui--text_color), box-sizing border-box
    - A submit button:
      - Use a `<button>` styled with background var(--hs-accent), color white, border none, border-radius 12px, padding 12px 24px, font-size 16px, width 100%, cursor pointer, opacity 0.5 when disabled
      - Text: "Send Feedback" (or "Sending..." when submitting)
      - Disabled when text is empty or submitting
    - On submit: call `apiFetch('/feedback', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text: text.trim() }) })`
    - On success (res.ok): set submitted true, clear text
    - On error: you can show a simple inline error message
  - Success state (when submitted is true):
    - Show "Thanks for the feedback!" message with a warm tone
    - Show a "Send More" button that resets submitted to false
  - No category picker, no emoji rating (per user decision)

**mini-app/src/pages/Hub.tsx:**
- Import `MessageSquare` from `lucide-react` (add to existing icon imports)
- Add a new Cell at the bottom of the Section (after the Meal Plan cell):
  - `before={<MessageSquare size={24} style={iconStyle} />}`
  - `subtitle="Share your thoughts"` (static text, no API data needed)
  - `onClick={() => navigate('/feedback')}`
  - `style={cellStyle}`
  - Text content: "Give Feedback"

**mini-app/src/router.tsx:**
- Import `Feedback` from `./pages/Feedback`
- Add route: `{ path: 'feedback', element: <Feedback /> }` after the plan route in the children array
  </action>
  <verify>
Run `cd /workspace/mini-app && npx tsc --noEmit` to verify the Mini App TypeScript compiles. Also run `npx tsc --noEmit` from the root for the server. Verify:
- `mini-app/src/pages/Feedback.tsx` exports Feedback component
- `mini-app/src/pages/Hub.tsx` has MessageSquare import and Give Feedback cell
- `mini-app/src/router.tsx` has /feedback route
  </verify>
  <done>
Mini App hub shows "Give Feedback" card with MessageSquare icon. Clicking navigates to /feedback route. Feedback page has textarea + submit button. Submitting calls POST /api/feedback. Success state shows "Thanks for the feedback!" with option to send more. Empty submissions prevented client-side.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for both root and mini-app
2. POST /api/feedback endpoint validates non-empty text and returns 400 for empty
3. POST /api/feedback saves with source "mini-app" to app_feedback table
4. Hub.tsx has 4 cards: Grocery List, Recipes, Meal Plan, Give Feedback
5. Feedback.tsx has textarea (not single-line input), submit button, and success state
6. Client router has /feedback path
7. No category picker, no emoji rating, no sentiment scoring (all deferred)
</verification>

<success_criteria>
- Give Feedback card appears on Mini App hub with MessageSquare icon
- Clicking navigates to /feedback page with textarea and submit button
- Submitting non-empty feedback succeeds, shows thank-you message, clears form
- Empty submissions are rejected (client-side disabled button + server-side 400)
- Feedback is saved to app_feedback table with source "mini-app"
- Both server and mini-app TypeScript compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/18-app-feedback/18-02-SUMMARY.md`
</output>
