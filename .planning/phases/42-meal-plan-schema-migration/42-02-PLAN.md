---
phase: 42-meal-plan-schema-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - src/ai/tools.ts
  - src/ai/tool-handler.ts
  - src/mini-app/routes/meal-plan.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02

must_haves:
  truths:
    - "Claude can create meal plans with any of the 6 meal types via save_meal_plan tool"
    - "Claude can log meals with any of the 6 meal types via log_meal tool"
    - "The Mini App API returns entries sorted by day, then chronological meal type, then insertion order"
    - "A single meal slot (day + meal_type) can hold multiple entries (multi-recipe) and they sort by ID"
    - "Existing dinner-only plans display correctly in the API response"
  artifacts:
    - path: "src/ai/tools.ts"
      provides: "Expanded enum in save_meal_plan and log_meal tool definitions"
      contains: "snack.*dinner.*dessert.*other"
    - path: "src/ai/tool-handler.ts"
      provides: "Updated MealType cast for expanded enum"
      contains: "MealType"
    - path: "src/mini-app/routes/meal-plan.ts"
      provides: "Updated CASE sort for 6 meal types + id ASC"
      contains: "WHEN 'snack' THEN 3"
  key_links:
    - from: "src/ai/tools.ts"
      to: "src/planning/repository.ts"
      via: "Tool enum values must match MealType union"
      pattern: "breakfast.*lunch.*snack.*dinner.*dessert.*other"
    - from: "src/ai/tool-handler.ts"
      to: "src/planning/repository.ts"
      via: "Casts meal_type string to MealType"
      pattern: "as MealType"
    - from: "src/mini-app/routes/meal-plan.ts"
      to: "meal_plan_entries table"
      via: "CASE statement maps meal_type to sort order"
      pattern: "CASE.*meal_type.*WHEN.*breakfast.*THEN 1"
---

<objective>
Update Claude tool definitions, tool handler, and Mini App API to support the expanded 6-type meal enum and correct multi-recipe sort ordering.

Purpose: Makes the expanded schema usable by both the AI agent (via tools) and the Mini App (via API).
Output: Updated tool definitions with 6-type enum, updated API sort with chronological meal type ordering + insertion-order within slots.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-meal-plan-schema-migration/42-01-SUMMARY.md

@src/ai/tools.ts
@src/ai/tool-handler.ts
@src/mini-app/routes/meal-plan.ts

<interfaces>
<!-- Types from Plan 01 (expanded) -->

From src/planning/repository.ts (after Plan 01):
```typescript
export type MealType = "breakfast" | "lunch" | "snack" | "dinner" | "dessert" | "other";
```

<!-- Current tool enum to expand -->

From src/ai/tools.ts (save_meal_plan tool):
```typescript
meal_type: {
  type: "string",
  enum: ["breakfast", "lunch", "dinner"],
  description: "Meal type, default: dinner",
},
```

From src/ai/tools.ts (log_meal tool):
```typescript
meal_type: {
  type: "string",
  enum: ["breakfast", "lunch", "dinner"],
  description: "Meal type (default: dinner)",
},
```

<!-- Current CASE in API route -->

From src/mini-app/routes/meal-plan.ts:
```sql
ORDER BY mpe.day_of_week ASC,
         CASE mpe.meal_type WHEN 'breakfast' THEN 1 WHEN 'lunch' THEN 2 WHEN 'dinner' THEN 3 END ASC
```

<!-- Tool handler cast -->

From src/ai/tool-handler.ts (line 680):
```typescript
mealType: (e.meal_type as MealType) ?? "dinner",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand tool definition enums for save_meal_plan and log_meal</name>
  <files>src/ai/tools.ts, src/ai/tool-handler.ts</files>
  <action>
1. In `src/ai/tools.ts`, find the `PLAN_TOOLS` array:
   - In the `save_meal_plan` tool definition, change the `meal_type` enum from `["breakfast", "lunch", "dinner"]` to `["breakfast", "lunch", "snack", "dinner", "dessert", "other"]`. Update the description to: `"Meal type (breakfast, lunch, snack, dinner, dessert, other). Default: dinner"`
   - In the `log_meal` tool definition, change the `meal_type` enum from `["breakfast", "lunch", "dinner"]` to `["breakfast", "lunch", "snack", "dinner", "dessert", "other"]`. Update the description to: `"Meal type (breakfast, lunch, snack, dinner, dessert, other). Default: dinner"`

2. In `src/ai/tool-handler.ts`:
   - No code changes needed. Line 680 already casts `e.meal_type as MealType` and defaults to `"dinner"`. Since `MealType` was expanded in Plan 01, this cast now correctly accepts all 6 values. The import `type { PlanEntry, MealType } from "../planning/repository.js"` on line 5 already imports MealType.
   - Line 787 in the `log_meal` handler uses `input.meal_type as string | undefined` which is passed to `logMeal()` in history.ts as `mealType?: string` -- also correct, no change needed.
   - Verify this by confirming the file compiles cleanly.
  </action>
  <verify>
    <automated>cd /workspace && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Both tool definitions show 6-value enum, tool-handler compiles with expanded MealType</done>
</task>

<task type="auto">
  <name>Task 2: Update Mini App API sort order for 6 meal types and multi-recipe slots</name>
  <files>src/mini-app/routes/meal-plan.ts</files>
  <action>
Update the SQL query in `src/mini-app/routes/meal-plan.ts` `getPlan()` method:

1. Expand the CASE statement from 3 to 6 meal types in chronological order per user decision:
   - breakfast = 1, lunch = 2, snack = 3, dinner = 4, dessert = 5, other = 6
   - Add `ELSE 7` fallback for any unknown values (defensive)

2. Add `mpe.id ASC` as the third sort key for insertion-order within a meal slot (multi-recipe ordering per user decision).

The updated ORDER BY clause should be:
```sql
ORDER BY mpe.day_of_week ASC,
         CASE mpe.meal_type
           WHEN 'breakfast' THEN 1
           WHEN 'lunch' THEN 2
           WHEN 'snack' THEN 3
           WHEN 'dinner' THEN 4
           WHEN 'dessert' THEN 5
           WHEN 'other' THEN 6
           ELSE 7
         END ASC,
         mpe.id ASC
```

No other changes to the route. The flat list response shape is preserved per user decision (no pre-grouping). Multiple entries with the same day + meal_type naturally form a multi-recipe slot in the response, and the Mini App will group client-side (Phase 44 work).
  </action>
  <verify>
    <automated>cd /workspace && npx tsc --noEmit && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>API query sorts by day ASC, 6-type chronological CASE ASC, id ASC. All tests pass. Flat response shape preserved.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes with no errors
- `npm test` passes (all existing tests unaffected)
- `grep -n "enum" src/ai/tools.ts` shows both save_meal_plan and log_meal have 6-value enums
- `grep -n "CASE" src/mini-app/routes/meal-plan.ts` shows 6 WHEN clauses + ELSE
- `grep -n "mpe.id ASC" src/mini-app/routes/meal-plan.ts` confirms insertion-order sort
</verification>

<success_criteria>
- save_meal_plan tool definition accepts 6 meal types
- log_meal tool definition accepts 6 meal types
- API sort order is: day ASC, chronological meal type (breakfast=1 through other=6) ASC, id ASC
- Multi-recipe slots (same day + meal_type, different entries) sort by insertion order
- All existing tests pass
- Flat response shape unchanged (no breaking API changes)
</success_criteria>

<output>
After completion, create `.planning/phases/42-meal-plan-schema-migration/42-02-SUMMARY.md`
</output>
