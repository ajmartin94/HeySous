---
phase: 45-grocery-reminders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/reminders/generator.ts
  - tests/reminders/generator.test.ts
autonomous: true
requirements: [PLAN-05, PLAN-06]

must_haves:
  truths:
    - "Start-cooking reminders fire for breakfast, lunch, snack, dessert, and other meal types -- not just dinner"
    - "Each meal type's reminder uses that type's configured time (breakfastTime, lunchTime, etc.) for offset calculation"
    - "No-recipe entries for any meal type still get reminders using the 45-min default offset"
    - "Start-cooking reminder context includes mealType so sender can adapt message tone"
    - "Grocery list generation still works across all meal types (no regression from reminder changes)"
  artifacts:
    - path: "src/reminders/generator.ts"
      provides: "All-meal-type start-cooking reminder generation"
      contains: "getMealTypeTime"
    - path: "tests/reminders/generator.test.ts"
      provides: "Tests for multi-meal-type reminder generation"
      contains: "breakfast"
  key_links:
    - from: "src/reminders/generator.ts"
      to: "src/reminders/types.ts"
      via: "ReminderSettings meal time fields"
      pattern: "settings\\.(breakfast|lunch|snack|dinner|dessert)Time"
    - from: "src/reminders/generator.ts"
      to: "src/reminders/sender.ts"
      via: "mealType in contextJson for start_cooking reminders"
      pattern: "mealType.*meal\\.mealType"
---

<objective>
Extend start-cooking reminders to fire for all meal types (breakfast, lunch, snack, dinner, dessert, other) using per-meal-type time lookups, and verify grocery list generation already aggregates all meal types.

Purpose: Currently only dinner entries get start-cooking reminders. Users who plan breakfasts, lunches, and snacks deserve timely "time to cook" nudges based on their configured meal times.

Output: Updated generator.ts with all-meal-type support and comprehensive tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-grocery-reminders/45-CONTEXT.md
@src/reminders/generator.ts
@src/reminders/types.ts
@src/reminders/sender.ts
@tests/reminders/generator.test.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/reminders/types.ts:
```typescript
export interface ReminderSettings {
  id: number;
  householdId: string;
  timezone: string;
  morningTime: string;
  breakfastTime: string;   // default "07:00"
  lunchTime: string;       // default "12:00"
  snackTime: string;       // default "15:00"
  dinnerTime: string;      // default "17:30"
  dessertTime: string;     // default "20:00"
  morningEnabled: boolean;
  prepAlertsEnabled: boolean;
  mutedUntil: Date | null;
  createdAt: Date;
  updatedAt: Date;
}
```

From src/reminders/sender.ts (buildReminderPrompt already handles mealType):
```typescript
case "start_cooking": {
  const recipeName = (context.recipeName as string) || "dinner";
  const mealType = (context.mealType as string) || "dinner";
  return `Generate a "time to start cooking" reminder for ${recipeName} (${mealType}).`;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend generator to create start-cooking reminders for all meal types</name>
  <files>src/reminders/generator.ts, tests/reminders/generator.test.ts</files>
  <behavior>
    - Test: Breakfast entry at breakfastTime 07:00 with 30-min recipe gets start-cooking reminder at 06:30
    - Test: Lunch entry at lunchTime 12:00 with no recipe (null knowledgeItemId) gets reminder at 11:15 (45-min default)
    - Test: Snack entry at snackTime 15:00 with 20-min recipe gets reminder at 14:40
    - Test: Dessert entry at dessertTime 20:00 gets reminder using dessertTime, not dinnerTime
    - Test: "other" meal type falls back to dinnerTime (no otherTime field exists)
    - Test: Multiple meal types on same day each get their own start-cooking reminder
    - Test: Existing dinner tests still pass (no regression)
  </behavior>
  <action>
1. Add a helper function `getMealTypeTime(settings: ReminderSettings, mealType: string): string` that maps meal types to their corresponding time field:
   - "breakfast" -> settings.breakfastTime
   - "lunch" -> settings.lunchTime
   - "snack" -> settings.snackTime
   - "dinner" -> settings.dinnerTime
   - "dessert" -> settings.dessertTime
   - default (including "other") -> settings.dinnerTime

2. In the start-cooking section (line 242-349), remove the `if (meal.mealType === "dinner")` guard at line 247. The for-loop should process ALL meals regardless of mealType.

3. Replace the hardcoded `settings.dinnerTime` lookup at line 311 with `getMealTypeTime(settings, meal.mealType)`.

4. Add `mealType: meal.mealType` to the start_cooking contextJson object (around line 341) so sender.ts can use it for tone adaptation. The sender already reads `context.mealType` -- it just needs the data.

5. Remove `Math.max(0, ...)` floor per user decision: "No floor on reminder time -- if the math says 5am for a 2-hour breakfast recipe at 7am, fire at 5am."

6. Update existing test settings objects to include all required ReminderSettings fields (breakfastTime, lunchTime, snackTime, dessertTime) -- they are currently missing these fields which will cause TypeScript errors.

7. Add new tests per the behavior block above. Use the existing test helper pattern (createDinnerPlan but generalized for any meal type -- rename or create createMealPlan helper that accepts mealType param).

8. Export `getMealTypeTime` for testability.
  </action>
  <verify>
    <automated>cd /workspace && npx vitest run tests/reminders/generator.test.ts</automated>
  </verify>
  <done>All existing tests pass with updated settings objects. New tests verify breakfast/lunch/snack/dessert/other meal types each get start-cooking reminders at the correct per-type time. The mealType field appears in start_cooking contextJson.</done>
</task>

<task type="auto">
  <name>Task 2: Verify grocery aggregation and run full type check</name>
  <files></files>
  <action>
1. Run `npm run typecheck` to confirm no TypeScript errors in the modified files or anywhere else.

2. Run the full test suite (`npm test`) to confirm no regressions across the codebase.

3. Verify PLAN-05 (grocery aggregation) by inspecting the system prompt grocery section and get_meal_plan tool -- confirm that Claude receives all meal types in plan context when generating grocery lists. This is a read-only verification: the system prompt at src/ai/system-prompt.ts instructs Claude to "get their active meal plan (via get_meal_plan)" before generating grocery lists, and get_meal_plan already returns all meal types (confirmed in Phase 42 work). No code changes needed for PLAN-05.
  </action>
  <verify>
    <automated>cd /workspace && npm run typecheck && npm test</automated>
  </verify>
  <done>TypeScript compiles cleanly. Full test suite passes. Grocery list generation path confirmed to already include all meal types via get_meal_plan tool (no code change required for PLAN-05).</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/reminders/generator.test.ts` -- all reminder generator tests pass
2. `npm run typecheck` -- no TypeScript errors
3. `npm test` -- full test suite passes
4. Manual code review: `grep -n "mealType.*dinner" src/reminders/generator.ts` should NOT show any dinner-only filter
5. Manual code review: `grep -n "getMealTypeTime" src/reminders/generator.ts` should show the new helper function
</verification>

<success_criteria>
- Start-cooking reminders are generated for ALL meal types (breakfast, lunch, snack, dinner, dessert, other)
- Each meal type uses its own configured time field from ReminderSettings for offset calculation
- mealType is included in start_cooking reminder contextJson
- No floor on reminder time (early morning reminders allowed)
- All tests pass including new multi-meal-type test cases
- TypeScript compiles cleanly
- PLAN-05 verified as already working (no code changes needed)
</success_criteria>

<output>
After completion, create `.planning/phases/45-grocery-reminders/45-01-SUMMARY.md`
</output>
