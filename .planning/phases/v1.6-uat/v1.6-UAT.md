---
status: diagnosed
phase: v1.6-all-day-meals-ux
source: 42-01-SUMMARY.md, 42-02-SUMMARY.md, 43-01-SUMMARY.md, 43-02-SUMMARY.md, 44-01-SUMMARY.md, 45-01-SUMMARY.md, 46-01-SUMMARY.md, 46-02-SUMMARY.md, 47-01-SUMMARY.md
started: 2026-03-04T15:00:00Z
updated: 2026-03-04T16:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Create a non-dinner meal plan
expected: Ask Claude to plan breakfast or lunch for a day. Claude saves it with the correct meal type (breakfast/lunch), not defaulting to dinner. Response references the specific meal type.
result: pass

### 2. Configure meal times via chat
expected: Tell Claude to change your breakfast time (e.g., "set my breakfast time to 8am"). Claude uses update_reminder_settings and confirms the change.
result: issue
reported: "Claude creates duplicate breakfast time preferences in knowledge base (both 7am and 8am entries) instead of updating the existing one when user changes breakfast time"
severity: major

### 3. Multi-meal plan view in Mini App
expected: Open the Mini App meal plan page when you have multiple meal types planned. Entries are grouped under labeled section headers (e.g., "Breakfast", "Lunch", "Dinner") with icons for each type.
result: issue
reported: "Recipe names need indentation under meal type headers to show hierarchy, and font size/styling needs differentiation so recipe names don't look like day headers"
severity: cosmetic

### 4. Day expand/collapse in Mini App
expected: On the meal plan page, tap a day row. It should collapse to show just a meal count summary. Tap again to expand. Chevron indicator rotates with the state.
result: pass

### 5. Meal type icons and colors
expected: In the Mini App meal plan, snack shows a cookie icon (green), dessert shows a cake icon (pink), and other shows utensils (gray). Each meal type has a distinct visual identity.
result: pass

### 6. Deep-link buttons after tool use
expected: After Claude saves a meal plan, grocery list, or recipe, an "Open in app" inline keyboard button appears below the response. Tapping it opens the Mini App to the relevant page.
result: issue
reported: "Useless 'Open in app:' text message sent as separate message instead of attaching buttons directly to the Sous response message"
severity: cosmetic

### 7. Recipe deep-link from chat
expected: When Claude references a specific recipe and attaches a deep link, tapping the button opens the Mini App Recipes page with that recipe's detail view already open.
result: pass

### 8. Start-cooking reminders for non-dinner meals
expected: If you have a breakfast or lunch planned, you receive a start-cooking reminder at the appropriate time offset from that meal's configured time (not dinner time).
result: pass

### 9. No emojis in Claude responses
expected: Claude's messages do not contain emoji characters. Responses use plain text, HTML formatting, and dashes for lists.
result: pass

### 10. Mini App font and responsive layout
expected: Mini App text uses system-ui font (native OS font). On tablet/desktop widths, horizontal padding increases (24px at 768px, 48px at 1024px, 80px at 1440px+).
result: issue
reported: "Responsive padding doesn't take effect on desktop/iPad -- Telegram webview constrains viewport width so media queries never trigger. Needs a different approach like max-width + centering."
severity: major

## Summary

total: 10
passed: 6
issues: 4
pending: 0
skipped: 0

## Gaps

- truth: "Claude updates existing preference when user changes breakfast time"
  status: failed
  reason: "User reported: Claude creates duplicate breakfast time preferences in knowledge base (both 7am and 8am entries) instead of updating the existing one when user changes breakfast time"
  severity: major
  test: 2
  root_cause: "Preference dedup similarity threshold (0.85) is too high for parameterized preferences. 'Breakfast Time: 7am' vs 'Breakfast Time: 8am' yields 71.4% Jaccard similarity (below 0.85), so save_knowledge creates a new item instead of returning duplicate_found."
  artifacts:
    - path: "src/ai/tool-handler.ts"
      issue: "Dedup threshold 0.85 too high for preferences with single parameter changes (line ~312)"
    - path: "src/knowledge/fts.ts"
      issue: "computeContentSimilarity returns 0.714 for near-identical preference summaries"
  missing:
    - "Lower preference dedup threshold to ~0.70, or implement category-based matching for time preferences"
  debug_session: ""

- truth: "Recipe names visually nested under meal type headers with clear hierarchy"
  status: failed
  reason: "User reported: Recipe names need indentation under meal type headers to show hierarchy, and font size/styling needs differentiation so recipe names don't look like day headers"
  severity: cosmetic
  test: 3
  root_cause: "meal-entry has no left padding (only padding: 6px 0). Recipe name font-size (15px) and color are nearly identical to day header (16px, 600 weight, same color). No visual nesting."
  artifacts:
    - path: "mini-app/src/components/meal-plan/meal-plan.css"
      issue: ".meal-entry lacks left padding, .meal-entry__name styling too similar to .day-row__header"
  missing:
    - "Add padding-left: 12px to .meal-entry"
    - "Reduce .meal-entry__name font-weight to 400 (vs day header 600)"
  debug_session: ""

- truth: "Deep-link buttons attached to Sous response message, not as separate message"
  status: failed
  reason: "User reported: Useless 'Open in app:' text message sent as separate message instead of attaching buttons directly to the Sous response message"
  severity: cosmetic
  test: 6
  root_cause: "Processor sends ctx.reply('Open in app:', { reply_markup: keyboard }) as separate message (line ~770) after stream finalization. Stream sender's finalize() doesn't accept reply_markup. Tool calls are analyzed post-finalization, so keyboard isn't available at finalize time."
  artifacts:
    - path: "src/pipeline/processor.ts"
      issue: "Deep-link buttons sent as separate ctx.reply after finalization (line ~770)"
    - path: "src/telegram/stream-sender.ts"
      issue: "finalize() doesn't accept or pass reply_markup parameter"
  missing:
    - "After finalize(), use ctx.api.editMessageText on the finalized message ID to add reply_markup with the keyboard instead of sending a new message"
  debug_session: ""

- truth: "Mini App layout adapts to wider viewports on desktop/iPad"
  status: failed
  reason: "User reported: Responsive padding doesn't take effect on desktop/iPad -- Telegram webview constrains viewport width so media queries never trigger. Needs a different approach like max-width + centering."
  severity: major
  test: 10
  root_cause: "Telegram's Mini App webview opens in a ~400px panel on desktop, so min-width media queries at 768/1024/1440px never trigger. The responsive padding is effectively dead code in the only environment this app runs in."
  artifacts:
    - path: "mini-app/src/components/Layout.css"
      issue: "Media queries at 768/1024/1440px never trigger in Telegram's ~400px webview"
  missing:
    - "Remove dead media queries, apply a fixed comfortable padding for the 400px viewport instead"
  debug_session: ""
