---
phase: 36-pipeline-efficiency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/pipeline/token-budget-guard.ts
  - src/pipeline/processor.ts
  - src/conversation/context-builder.ts
  - src/bot/messages.ts
autonomous: true
requirements:
  - SEC-01
  - CFG-01

must_haves:
  truths:
    - "Household exceeding daily token budget receives a friendly canned message instead of triggering a Claude API call"
    - "Conversation sessions reset at midnight in the configured timezone, not after 4 hours of inactivity"
    - "DAILY_TOKEN_BUDGET and SESSION_TIMEZONE are configurable via environment variables"
    - "Non-AI interactions (commands, mini app) continue working when budget is exhausted"
  artifacts:
    - path: "src/pipeline/token-budget-guard.ts"
      provides: "Daily token budget checking per household"
      exports: ["checkDailyTokenBudget"]
    - path: "src/config.ts"
      provides: "New config fields for daily token budget and session timezone"
      contains: "dailyTokenBudget"
    - path: "src/bot/messages.ts"
      provides: "Canned rate limit message"
      exports: ["getDailyLimitMessage"]
  key_links:
    - from: "src/pipeline/processor.ts"
      to: "src/pipeline/token-budget-guard.ts"
      via: "checkDailyTokenBudget call before Claude API call"
      pattern: "checkDailyTokenBudget"
    - from: "src/conversation/context-builder.ts"
      to: "src/config.ts"
      via: "SESSION_TIMEZONE used for midnight boundary calculation"
      pattern: "sessionTimezone|SESSION_TIMEZONE"
---

<objective>
Enforce per-household daily token budgets and replace the hardcoded 4-hour session timeout with midnight-aligned calendar day sessions.

Purpose: Prevent runaway API costs with a per-household daily token budget (safety net, not restrictive). Replace the arbitrary 4-hour inactivity timeout with a clean midnight reset that aligns budget and session boundaries.

Output: Token budget guard module, config env vars, canned limit message, midnight session boundary in context builder, processor wiring.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/config.ts
@src/pipeline/processor.ts
@src/conversation/context-builder.ts
@src/bot/messages.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config env vars, token budget guard module, and canned message</name>
  <files>
    src/config.ts
    src/pipeline/token-budget-guard.ts
    src/bot/messages.ts
  </files>
  <action>
1. In `src/config.ts`:
   - Add `dailyTokenBudget: number` to Config interface (default 500_000)
   - Add `sessionTimezone: string` to Config interface (default "America/New_York")
   - Read from `DAILY_TOKEN_BUDGET` and `SESSION_TIMEZONE` env vars
   - dailyTokenBudget: `Number(process.env.DAILY_TOKEN_BUDGET) || 500_000`
   - sessionTimezone: `process.env.SESSION_TIMEZONE ?? "America/New_York"`

2. Create `src/pipeline/token-budget-guard.ts`:
   - Export `checkDailyTokenBudget(sqlite: BetterSqlite3.Database, householdId: string, budgetTokens: number, timezone: string): { allowed: boolean; tokensUsed: number; budgetTokens: number }`
   - Query token_usage table: sum (input_tokens + output_tokens + cache_creation_tokens + cache_read_tokens) WHERE household_id = ? AND created_at >= midnight-today-in-timezone
   - To get midnight timestamp: use the timezone to compute today's start-of-day as a Unix timestamp. Use `new Date()` with timezone-aware date string construction: get today's date string in the timezone (reuse getTodayInTimezone pattern from clock.ts), then parse "YYYY-MM-DDT00:00:00" and convert to epoch seconds.
   - The created_at column in token_usage is a Drizzle timestamp (stored as integer epoch seconds).
   - Return `{ allowed: tokensUsed < budgetTokens, tokensUsed, budgetTokens }`
   - Import type BetterSqlite3 from "better-sqlite3"
   - Use raw SQLite prepared statement (same pattern as preferences.ts, fts.ts)

3. In `src/bot/messages.ts`:
   - Add `getDailyLimitMessage(): string` to the existing message functions
   - Return a brief, friendly, OUT-OF-CHARACTER system notice (not Sous persona per user decision)
   - Example: "You've reached your daily message limit. Your limit resets at midnight -- feel free to keep using commands, the Mini App, and your grocery list in the meantime!"
   - Add 2-3 variants like other message functions (pickRandom pattern)
  </action>
  <verify>
    - `npm run typecheck` passes
    - `grep -n "dailyTokenBudget" src/config.ts` shows the new field
    - `grep -n "checkDailyTokenBudget" src/pipeline/token-budget-guard.ts` shows the export
    - `grep -n "getDailyLimitMessage" src/bot/messages.ts` shows the message function
  </verify>
  <done>
    Config reads DAILY_TOKEN_BUDGET and SESSION_TIMEZONE env vars. Token budget guard queries token_usage for today's spend. Canned rate limit message exists with variants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire budget check into processor and replace session boundary with midnight reset</name>
  <files>
    src/pipeline/processor.ts
    src/conversation/context-builder.ts
  </files>
  <action>
1. In `src/pipeline/processor.ts`:
   - Import `checkDailyTokenBudget` from `../pipeline/token-budget-guard.js`
   - Import `getDailyLimitMessage` from `../bot/messages.js`
   - After the message length check (step b1) and BEFORE saving the message to DB (step c), add budget enforcement:
     ```
     const budgetCheck = checkDailyTokenBudget(deps.sqlite, householdId, config.dailyTokenBudget, config.sessionTimezone);
     if (!budgetCheck.allowed) {
       log.info({ chatId, householdId, tokensUsed: budgetCheck.tokensUsed, budget: budgetCheck.budgetTokens }, "Daily token budget exhausted");
       try { await ctx.reply(getDailyLimitMessage()); } catch { /* best-effort */ }
       return;
     }
     ```
   - This placement means: the message is NOT saved to conversation history and Claude is NOT called when budget is exhausted. Bot commands and Mini App routes are unaffected since they don't go through the processor.
   - The budget check uses config.sessionTimezone for the midnight boundary calculation.

2. In `src/conversation/context-builder.ts`:
   - Replace the hardcoded `SESSION_GAP_MS = 4 * 60 * 60 * 1000` with a midnight-based session boundary
   - Change the function signature of `buildConversationContext` to accept an optional `sessionTimezone` parameter: `buildConversationContext(turns, tokenBudget, sessionTimezone?: string)`
   - Default timezone to "America/New_York" if not provided (backward compatible)
   - Replace the gap-based session check with a midnight-based check: compute the start-of-today in the given timezone (same approach as token-budget-guard), and filter out all turns whose createdAt is before midnight today
   - This means all messages from midnight to now are in the current session, regardless of gaps
   - Remove the `SESSION_GAP_MS` constant entirely
   - In the processor, pass `config.sessionTimezone` as the third argument to buildConversationContext

3. In `src/pipeline/processor.ts`:
   - Update the buildConversationContext call (step e) to pass `config.sessionTimezone`:
     ```
     const { messages: priorMessages, wasTruncated, originalTurnCount, includedTurnCount } = buildConversationContext(
       turns,
       CONVERSATION_TOKEN_BUDGET,
       config.sessionTimezone,
     );
     ```
  </action>
  <verify>
    - `npm run typecheck` passes
    - `npm test` passes (update any tests in tests/conversation/ or tests/pipeline/ if they rely on the 4-hour gap logic)
    - `grep -n "SESSION_GAP_MS" src/conversation/context-builder.ts` returns nothing (constant removed)
    - `grep -n "checkDailyTokenBudget" src/pipeline/processor.ts` shows the budget check is wired in
    - `grep -n "sessionTimezone" src/pipeline/processor.ts` shows timezone is passed to context builder
  </verify>
  <done>
    Processor enforces daily token budget before Claude calls. Session boundary uses midnight in configured timezone instead of 4-hour inactivity gap. All existing tests pass with necessary updates.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with zero errors
2. `npm test` passes -- update any context-builder tests that relied on 4-hour session gap
3. Budget check runs before Claude API call in processor
4. Session boundary is midnight-based, not gap-based
5. DAILY_TOKEN_BUDGET and SESSION_TIMEZONE env vars are read from config
</verification>

<success_criteria>
- A household that has used >= 500K tokens today gets a friendly canned message instead of a Claude call
- Session boundary is midnight in the configured timezone, replacing the 4-hour inactivity timeout
- Both values are configurable via env vars without code changes
- Bot commands (/help, /grocery, /plan) and Mini App routes still work when budget is exhausted
</success_criteria>

<output>
After completion, create `.planning/phases/36-pipeline-efficiency/36-01-SUMMARY.md`
</output>
