---
phase: 11-mini-app-foundation
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - src/bot/handlers/grocery.ts
  - src/bot/handlers/plan.ts
  - src/bot/index.ts
  - src/telegram/menu-button.ts
  - src/main.ts
  - package.json
autonomous: true
user_setup:
  - service: botfather
    why: "BotFather menu button can be set programmatically via bot.api.setChatMenuButton(), but the MINI_APP_URL must be a public HTTPS URL. User needs to set MINI_APP_URL env var."
    env_vars:
      - name: MINI_APP_URL
        source: "Your deployment URL (e.g., https://your-domain.com/app). Must be HTTPS. For local dev, use ngrok or cloudflared tunnel."

must_haves:
  truths:
    - "User taps 'View List' inline button in a grocery response and Mini App opens at /app/grocery"
    - "User taps 'View Plan' inline button in a meal plan response and Mini App opens at /app/plan"
    - "BotFather menu button opens the Mini App hub at /app/"
    - "Inline keyboard buttons only appear when MINI_APP_URL is configured"
    - "Root build:all script builds both backend and mini-app"
  artifacts:
    - path: "src/bot/handlers/grocery.ts"
      provides: "Inline keyboard with View List web_app button"
      contains: "webApp.*View List"
    - path: "src/bot/handlers/plan.ts"
      provides: "Inline keyboard with View Plan web_app button"
      contains: "webApp.*View Plan"
    - path: "src/telegram/menu-button.ts"
      provides: "BotFather menu button setup function"
      exports: ["setupMenuButton"]
    - path: "src/main.ts"
      provides: "Menu button setup called on startup"
      contains: "setupMenuButton"
  key_links:
    - from: "src/bot/handlers/grocery.ts"
      to: "config.miniAppUrl"
      via: "InlineKeyboard.webApp('View List', miniAppUrl + '/grocery')"
      pattern: "webApp.*miniAppUrl.*grocery"
    - from: "src/telegram/menu-button.ts"
      to: "bot.api.setChatMenuButton"
      via: "Sets menu button type web_app with miniAppUrl"
      pattern: "setChatMenuButton"
    - from: "src/main.ts"
      to: "src/telegram/menu-button.ts"
      via: "Calls setupMenuButton after bot creation"
      pattern: "setupMenuButton"
---

<objective>
Wire the bot to the Mini App: add inline keyboard buttons for deep-linking to specific views, configure the BotFather menu button for the hub, and ensure the full build pipeline works end-to-end.

Purpose: This connects the conversational bot (v1.0) to the new visual UI (v1.1). Users reach the Mini App through natural interaction points -- contextual buttons in bot responses and the menu button in the chat header.
Output: Bot responses include "View List" / "View Plan" web_app buttons. Menu button opens the hub. Build scripts produce a deployable package.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mini-app-foundation/11-RESEARCH.md
@.planning/phases/11-mini-app-foundation/11-CONTEXT.md
@.planning/phases/11-mini-app-foundation/11-01-SUMMARY.md
@.planning/phases/11-mini-app-foundation/11-02-SUMMARY.md

@src/bot/handlers/grocery.ts
@src/bot/handlers/plan.ts
@src/bot/index.ts
@src/main.ts
@src/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline keyboard buttons for deep-linking</name>
  <files>
    src/bot/handlers/grocery.ts
    src/bot/handlers/plan.ts
  </files>
  <action>
Update `src/bot/handlers/grocery.ts` -- `createGroceryHandler`:
- Import config from `../../config.js`
- Import InlineKeyboard from `grammy`
- In the /grocery command handler, AFTER sending the text grocery list reply, add a web_app inline button:
  - Only add if `config.miniAppUrl` is non-empty (graceful fallback when not configured)
  - Create: `new InlineKeyboard().webApp('View List', config.miniAppUrl + '/grocery')`
  - The webApp button tells Telegram to open the URL in a WebView
  - This button should be ADDED to the existing keyboard, not replace it. The existing grocery keyboard has check-off buttons. Add the "View List" webApp button as an additional row.
  - Use: `keyboard.row().webApp('View List', config.miniAppUrl + '/grocery')` to add it as a new row below the existing check-off buttons
- Note: the grocery handler already uses buildGroceryKeyboard() which returns an InlineKeyboard. Chain `.row().webApp(...)` onto that existing keyboard before passing to reply_markup.

Update `src/bot/handlers/plan.ts` -- `createPlanHandler`:
- Import config from `../../config.js`
- Import InlineKeyboard from `grammy` (if not already imported)
- In the /plan command handler, after the reply showing the meal plan text, add a web_app button:
  - Only add if `config.miniAppUrl` is non-empty
  - Create: `new InlineKeyboard().webApp('View Plan', config.miniAppUrl + '/plan')`
  - Attach as reply_markup on the existing plan reply
- Read the existing plan handler to understand its reply pattern and add the button appropriately

LOCKED DECISIONS enforced:
- Contextual inline keyboard buttons -- grocery response gets "View List", meal plan gets "View Plan"
- Deep-link directly to the relevant view -- "View List" opens /grocery, not the hub
- Buttons use web_app URL field, not startapp parameter (per research recommendation)
  </action>
  <verify>
Run `npx tsc --noEmit` to verify TypeScript compiles. Read the modified files to confirm the webApp buttons are correctly formed with the right URL paths.
  </verify>
  <done>
Grocery responses include "View List" button opening /app/grocery in WebView. Plan responses include "View Plan" button opening /app/plan. Buttons only appear when MINI_APP_URL is configured. Existing functionality (check-off buttons for grocery, text display for plan) preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Menu button setup + build pipeline</name>
  <files>
    src/telegram/menu-button.ts
    src/main.ts
    package.json
  </files>
  <action>
Create `src/telegram/menu-button.ts`:
- Export async function `setupMenuButton(bot: Bot<BotContext>, miniAppUrl: string): Promise<void>`
- Import Bot and BotContext types appropriately
- If miniAppUrl is empty, log a warning and return (no-op in dev without tunnel)
- Call `bot.api.setChatMenuButton()` with:
  ```typescript
  {
    menu_button: {
      type: 'web_app',
      text: 'Open App',
      web_app: { url: miniAppUrl },
    }
  }
  ```
- The miniAppUrl should point to the hub (e.g., `https://domain.com/app`) since the menu button opens the hub (locked decision)
- Wrap in try/catch -- log error but don't crash if setChatMenuButton fails (might fail in polling mode without webhook configured)
- Import logger from `../logger.js`
- Log success: `logger.info({ url: miniAppUrl }, "BotFather menu button configured")`

Update `src/main.ts`:
- Import `setupMenuButton` from `./telegram/menu-button.js`
- After bot creation (`createBot(...)`) and before `bot.start()` or `app.listen()`, call:
  ```typescript
  await setupMenuButton(bot, config.miniAppUrl);
  ```
- Place this AFTER the bot is created but BEFORE starting polling/webhook
- This runs on every startup, which is idempotent (setChatMenuButton replaces any existing setting)

Update root `package.json` scripts (if not already done by Plan 02):
- Ensure `"build:app": "cd mini-app && npm run build"` exists
- Ensure `"build:all": "npm run build && npm run build:app"` exists
- These enable a single command to build both backend TypeScript and frontend Vite SPA

LOCKED DECISIONS enforced:
- BotFather menu button opens the hub page (not a specific Mini App view)
- Menu button text: "Open App" (short, clear, fits Telegram's UI)

INFRA-07 COMPLETE: BotFather menu button configured to open primary Mini App URL.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify TypeScript compiles with new menu-button.ts and updated main.ts. Verify `npm run build:all` (or the individual build commands) completes successfully -- both backend tsc and frontend vite build.
  </verify>
  <done>
Menu button setup function exists and is called on startup. In webhook mode with MINI_APP_URL configured, the BotFather menu button opens the hub. Build pipeline compiles both backend and frontend. Graceful no-op when MINI_APP_URL is not set (dev/polling mode).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors across all modified files
2. `npm test` -- all existing tests pass
3. Grocery handler includes `webApp('View List', ...)` with URL pointing to /grocery
4. Plan handler includes `webApp('View Plan', ...)` with URL pointing to /plan
5. `src/telegram/menu-button.ts` calls `setChatMenuButton` with `type: 'web_app'`
6. `src/main.ts` calls `setupMenuButton` after bot creation
7. Root package.json has `build:app` and `build:all` scripts
8. Buttons conditionally appear only when MINI_APP_URL is non-empty
</verification>

<success_criteria>
- INFRA-01: Inline keyboard buttons launch Mini App WebView at correct deep-linked routes
- INFRA-07: BotFather menu button opens the hub page
- Build pipeline produces deployable backend + frontend from a single command
- All existing bot functionality preserved (no regressions in grocery check-off, plan display, etc.)
- Graceful degradation when MINI_APP_URL is not configured (buttons simply don't appear)
</success_criteria>

<output>
After completion, create `.planning/phases/11-mini-app-foundation/11-03-SUMMARY.md`
</output>
